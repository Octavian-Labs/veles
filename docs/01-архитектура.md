# Архитектура и роадмап проекта РЯП

> Дата создания: 2026-09-19

## Цель

Собственная полностью русская система программирования «с нуля»:
русский ассемблер → русский C-подобный язык → русский C++-подобный язык,
с генерацией настоящего машинного кода x86-64 и исполняемых файлов
под разные платформы.

## Принятые решения

- **Целевой процессор:** реальный x86-64 (AMD64). Никаких виртуальных машин —
  ассемблер генерирует настоящие байты машинного кода.
- **Первый ассемблер написан на Python.** Это инструмент раскрутки:
  важно не «на чём написан», а что он производит — реальный низкоуровневый
  код с русскими мнемониками.
- **Переносимость** достигается целями сборки: один исходник на РАСМ
  ассемблируется под Windows (PE64 + WinAPI) и Linux (ELF64 + syscall).
  Позже: macOS (Mach-O) и ARM64.
- **Честная оговорка:** один и тот же бинарник не может исполняться на всех
  ОС — разные форматы файлов и интерфейсы ядра. Переносимость — на уровне
  исходного текста и единого ассемблера.

## Этапы (роадмап)

| Этап | Содержание | Статус |
|------|-----------|--------|
| 1 | Ассемблер РАСМ: лексер, парсер, кодировщик x86-64, эмиттер PE64 | **готово (MVP)** |
| 2 | Эмиттер ELF64 (Linux, syscall) | **готово** |
| 3 | Язык «СИ-РУС»: C-подобный, русские ключевые слова (если/иначе/пока/верни, цел/сим...), компилятор → РАСМ или прямо в машкод | план |
| 4 | Язык «СИ++-РУС»: классы, объекты, перегрузка — осмысленное подмножество | план |
| 5 | Самораскрутка: компилятор СИ-РУС переписывается на СИ-РУС | план |
| 6 | Бэкенды: macOS (Mach-O), ARM64 | план |

## Структура репозитория

```
ryap/
  src/asm_rus/        # ассемблер РАСМ (Python)
    lexer.py          # токены
    expr.py           # выражения (метки, $, арифметика)
    parser.py         # операторы и операнды
    regs.py           # регистры + русские псевдонимы
    encoder.py        # кодировщик x86-64
    assembler.py      # двухпроходный драйвер
    emit_pe.py        # генератор PE64 (Windows)
    emit_elf.py       # генератор ELF64 (Linux)
    __main__.py       # CLI
  examples/           # примеры .асм
  tests/              # pytest
  docs/               # документация
  run_checks.ps1      # автопроверки (black, flake8, pytest)
  .github/workflows/ci.yml
```

## Git-процесс

- `main` — стабильная версия, `develop` — рабочая, `feature/*` — функции.
- Каждое изменение — отдельной веткой от `develop`, слияние после проверок.
