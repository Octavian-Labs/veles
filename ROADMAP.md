# ВЕЛЕС — дорожная карта

Дата сверки: 2026-09-21
Статус: действующий долгосрочный план

Точная очередь ближайших PR находится в [ЗАДАЧИ.md](ЗАДАЧИ.md), памятка
новой сессии — в [AGENTS.md](AGENTS.md). Канонические решения — в
[docs/05-канон.md](docs/05-канон.md), разделы 11–15.

## Пройденные вехи

- [x] **ИСКРА x86-64** — русский ассемблер: мнемоники, регистры,
  выражения, секции, метки, псевдоинструкции `ВХОДКАДРА`/`ВЫХОДКАДРА`.
- [x] **Форматы исполняемых** — PE64 (Windows), ELF64 (Linux),
  Mach-O x86-64 (macOS, dyld-путь с `LC_MAIN` + резерв под подпись).
- [x] **РАЗУМ** — самохостящийся компилятор: точечные типы
  `цел.верх`/`нат.рост`, модули `взять сист.файл`, записи, перечисления,
  вложенные `искра:`-блоки, пипхол-оптимизатор.
- [x] **Три ОС** — `--цель windows|linux|macos`, платформенные модули
  `биб/*.windows|.linux|.macos.раз` (консоль, файл, процесс, система).
- [x] **Самосборка** — сиды цепочки сборки в репозитории; фикс-поинт
  (два поколения — идентичная ИСКРА) проверяется в CI на трёх ОС.
- [x] **Строгий `лог`** — условия и логические операторы только для `лог`,
  нормализация до `нет`/`да`, матрица тестов в CI.
- [x] **СЛЕД, базовый срез** — машинный отладчик Win32 Debug API; развитие
  и подтверждённые дефекты ведутся в [след/ЗАДАЧИ.md](след/ЗАДАЧИ.md).
- [x] **Расширение VSCode 0.2.0** — грамматики по канону, точечные
  типы и модули, вложенные `искра:`-блоки, сниппеты.

## Текущие вехи

### Веха: базовая безопасность РАЗУМА (канон §15)

**Текущий следующий PR:** `фича/границы-массивов`. Каждый пункт выполняется
отдельной веткой и отдельным PR; безопасность типов не ослабляется режимом
сборки или оптимизацией. Приёмочные критерии каждого шага находятся в
[ЗАДАЧИ.md](ЗАДАЧИ.md).

- [x] Строгий `лог`: только `нет`/`да`; условия и `не`/`и`/`или` требуют
  `лог`; числа и указатели проверяются явным сравнением; `как лог` для них
  запрещено; значения нормализуются при чтении и записи.
- [x] `--режим надёжно|быстро`, по умолчанию `надёжно`; режим безопасности
  независим от `-O0/-O1`; выбранный режим хранится в генераторе и
  фиксируется прагмой `;режим` в сгенерированной ИСКРЕ.
- [ ] Проверки границ массивов с ранней статической диагностикой и
  однократным вычислением базы и индекса.
- [ ] Безопасные срезы и строки: порядок проверок до арифметики длины,
  байтовая индексация UTF-8, предупреждение для сырых указателей.
- [ ] Стабильные обработчики `стд_сбой_границы`/`стд_сбой_среза` и коды
  завершения 101/102 на Windows, Linux и macOS.
- [ ] Доказуемое удаление лишних проверок без ослабления семантики.
- [ ] `--память проверка`: активные выделения, защитные значения,
  карантин, двойное освобождение и отчёт об утечках.
- [ ] Типы `размер`/`смещение` и поэтапная миграция машинных размеров.
- [ ] Видимость модулей и интерфейсный кэш `.рми`.

### Веха: версии без слоёв (канон §11)

- [ ] `;версия N` в лексере РАЗУМА и ИСКРЫ — файл читается под
  семантикой своей версии; поддерживаются последние 3.
- [ ] Конвейер устаревания в коде: `:=`, `цел8…без64`, `бул`,
  `д32/д64` — предупреждение → ошибка → удаление.
- [ ] `переведи` — инструмент автоматической миграции исходников
  на актуальный канон (программа на РАЗУМЕ).
- [ ] `тесты/канон/версия_N/` — совместимость каждой поддерживаемой
  версии как тест.
- [ ] Отметки `;стабильно`/`;развивается` в модулях `биб/`.

### Веха: `вещ.*`, `век` и SIMD

- [ ] SSE-кодогенерация в `иск/кодер.раз` (xmm-регистры, скалярные
  операции, соглашение вызова с плавающей точкой).
- [ ] Типы `вещ.рост`/`вещ.верх` в РАЗУМЕ, `мат.вещ.*`.
- [ ] Половинная точность: `вещ.шаг` (f16) и `вещ.шагм` (bf16) —
  форматы нейросетевых нагрузок, где 32/64 бита избыточны.
- [ ] Тип `век <элемент> х N` — вектор фиксированной ширины поверх
  SSE/AVX; поэлементные операции и редукции в `мат.век.*`.
- [ ] Уровень `пик` (128 бит, включая `вещ.пик`) — целевое имя,
  реализация после `вещ.*`.

### Веха: редактор

- [ ] VSCode tasks: сборка РАЗУМА/ИСКРЫ, прогон тестов.
- [ ] Folding/outline для `функ`/`запись`/`переч`/`искра:`.
- [ ] Hover-документация и базовое автодополнение.
- [ ] Veles Language Server: семантические токены, переход к
  определению, диагностика, переименование.
- [ ] Отладочные метаданные РАЗУМА (`--отладка`, канон §14): типы,
  имена и соответствие строк отдельной секцией/файлом для СЛЕДа и
  LSP — вместо рантайм-рефлексии.

## Большие вехи

### Веха: ОС на ВЕЛЕСЕ (голое железо)

Загрузка полностью средствами ВЕЛЕСА, без сторонних загрузчиков.

- [ ] ИСКРА: порты `ВВОД`/`ВЫВОД`, управление ЦП (`ЗАПР`/`РАЗР`,
  `ВОЗВПР`, `ОЖИД`), системные таблицы (`ЛГДТ`/`ЛИДТ`, `ПЕР CR*`),
  `ЦПУИД`, `ЧТМСР`/`ЗПМСР`, `ОТМЕНИ`, сегментные регистры,
  дальний прыжок; `;формат плоский` + `ОРГ`.
- [ ] Цель `uefi-x86_64`: subsystem EFI в `иск/пе.раз`,
  `биб/ос/уэфи.раз` (BootServices, GOP-фреймбуфер, чтение ядра
  с ESP, `ExitBootServices`).
- [ ] Позже — BIOS-путь: `БИТЫ 16/32` в ИСКРЕ, MBR-загрузчик,
  стаб protected→long mode.
- [ ] `образ.раз` — сборка ESP/дискового образа программой на РАЗУМЕ.
- [ ] `биб/ос/`: порты, ком (COM1), экран, гдт, идт + исключения,
  пик → лапик/иоапик, физпам (битовая карта), виртпам, куча,
  клава (PS/2), пит/таймер, ртс.
- [ ] Планировщик: контексты задач, кооперативка → вытеснение
  по таймеру.
- [ ] Диск: псие (PCI) → ата (PIO) → ахси; вфс + ramfs/своя ФС.
- [ ] ring-3 + `СИСВХОД`/`СИСВЫХОД` + загрузчик ELF: ОС запускает
  программы, собранные РАЗУМОМ.
- [ ] CI: smoke-тест «ядро напечатало в COM1» в QEMU + OVMF.

### Веха: параллелизм (канон §14)

- [ ] `ЗАМОК`, `СРАВОБМЕН`, `ОБМЕНДОБ`, ограды в ИСКРЕ.
- [ ] Тип `атом` и модуль `атом` в биб; порядки
  `слабый`/`захват`/`отпуск`/`полный`.
- [ ] `задача`/`исполнитель` — пулы ядер, задел под гетерогенные
  машины (P/E-ядра).
- [ ] SMP в ОС: INIT-SIPI-SIPI, per-CPU данные, спинлоки.

### Веха: новые машины (канон §12)

- [ ] Структура `иск/кодер/х86_64.раз` — вынести архитектуру в модуль.
- [ ] `--цель` = пара система-машина; суффиксы машин в `биб/`.
- [ ] Кодер `а64` (ARM64) — второй кодер, проверка идеи общего текста ИСКРЫ.
- [ ] Кодер `рв64` (RISC-V).

### Веха: приборы (канон §13)

- [ ] Домены памяти в типе указателя; запрет неявного приведения.
- [ ] Интерфейс `область` — единый аллокатор по доменам.
- [ ] `биб/прибор` — загрузка/исполнение программ ускорителя
  (GPU/NPU/TPU) как данных или через диалект ИСКРЫ устройства.

### Веха: квант (канон §13)

- [ ] `биб/квант` — построение квантовой схемы как данные.
- [ ] Эмиттер квантового формата (модуль, как `эльф.раз`/`пе.раз`).
- [ ] Отправка схемы на исполнение через рантайм прибора.

### Веха: ЯР

- [ ] Спецификация v0.2 по опыту РАЗУМА (канон имён един).
- [ ] Компилятор ЯР → РАЗУМ или → ИСКРА напрямую.

## Принципы движения

- Каждая веха — ветка от `develop`, PR, зелёный CI на трёх ОС.
- Устаревшее уходит по конвейеру §11 — новое не наслаивается на старое.
- Ядро РАЗУМА остаётся маленьким: всё возможное живёт в `биб/`.
- Новая архитектура = новый кодер + машинные модули; РАЗУМ не трогаем.
