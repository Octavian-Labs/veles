;версия 1
# переведи: устаревшие формы → канон
функ главная() цел.верх:
    х нат.старт = 5
    введи у = 36
    флаг лог = да
    если флаг:
        искра:
            ПЕР АКК, 0        ; без64 := бул — тут не трогаем
        верни х + у           # 41, и цел32 тут не трогаем
    верни 0
