
взять основа
функ главная() цел64:
    с *Список = список_новый()
    список_добавь(с, 10)
    список_добавь(с, 20)
    список_добавь(с, 30)
    сумма без64 = 0
    для i в 0..с.длина:
        сумма += список_получи(с, i)
    список_свободен(с)
    верни сумма как цел64
