
взять основа
функ главная() цел.верх:
    список *Список = список_новый()
    список_добавь(список, 10)
    список_добавь(список, 20)
    список_добавь(список, 30)
    сумма нат.верх = 0
    для инд в 0..список.длина:
        сумма += список_получи(список, инд)
    список_свободен(список)
    верни сумма как цел.верх
