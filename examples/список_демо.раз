
взять основа
функ главная() цел.верх:
    с *Список = список_новый()
    список_добавь(с, 10)
    список_добавь(с, 20)
    список_добавь(с, 30)
    сумма нат.верх = 0
    для инд в 0..с.длина:
        сумма += список_получи(с, инд)
    список_свободен(с)
    верни сумма как цел.верх
