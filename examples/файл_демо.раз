# Демо стдлиб «основа»: запись файла, чтение обратно, список, поиск в строке.
взять основа

функ главная() цел.верх:
    ок := файл_запиши("veles_test.txt", "раз два три 42")
    если не ок:
        печатьстр("ошибка записи")
        верни 1
    с := файл_прочти("veles_test.txt")
    если с == пусто:
        печатьстр("ошибка чтения")
        верни 1
    печать("прочитано: ")
    печатьстр(с)

    # поиск подстроки и разбор числа
    i := стр_найди(с, "42")
    печать("42 найдено на позиции: ")
    печатьцел(i)
    печатьстр("")
    верни стр_число(стр_срез(с, i как нат.верх, с.длина))
