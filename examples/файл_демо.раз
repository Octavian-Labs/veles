# Демо стдлиб «основа»: запись файла, чтение обратно, список, поиск в строке.
взять основа

функ главная() цел.верх:
    ок лог = файл_запиши("veles_test.txt", "раз два три 42")
    если не ок:
        печатьстр("ошибка записи")
        верни 1
    текст стр = файл_прочти("veles_test.txt")
    если текст == пусто:
        печатьстр("ошибка чтения")
        верни 1
    печать("прочитано: ")
    печатьстр(текст)

    # поиск подстроки и разбор числа
    поз цел.верх = стр_найди(текст, "42")
    печать("42 найдено на позиции: ")
    печатьцел(поз)
    печатьстр("")
    верни стр_число(стр_срез(текст, поз как нат.верх, текст.длина))
