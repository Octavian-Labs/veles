# ВЕЛЕС — дорожная карта оптимизации кода

Дата создания: 2026-09-22
Статус: действующий план производительности
Ветка: `фича/век`

Этот документ — рабочий план второго рода: не новые возможности языка
(это [ROADMAP.md](ROADMAP.md)), а качество машинного кода, который
порождает цепочка РАЗУМ → ИСКРА. Повод — измеренный профиль
нейросетевых примеров `_ш5м.раз` и `_сегм30м.раз` (U-Net, 31M
параметров): внутренний цикл свёртки генерирует ~57 инструкций там,
где достаточно ~10.

## Диагноз

ИСКРА уже умеет почти всё необходимое:

- полная SIB-адресация `[база + индекс*масштаб + смещ]`
  (`иск/кодер.раз`, кодирование ModRM/SIB);
- пакетные `МАКСПС/МИНПС/МАКСПД/МИНПД`, `ПСДВИГД`, арифметика ПС/ПД;
- `АДР` (LEA) для адресной арифметики без флагов.

Генератор РАЗУМА этим не пользуется и работает как стековая машина
на двух регистрах (АКК/СЧТ) с прогоном временных через стек —
включая фиктивный xmm-стек (`ВЫЧ УКС,16; ПЕРПС [УКС]; ДОБ УКС,16`).

## Ориентиры измерения

| Программа | Было | Сейчас (A1–A8+Б1) | Цель |
|---|---:|---:|---:|
| `_ш5м.раз` (5M MLP, точный SGD) | ~14,7 с | ~14 с (шум ±7%) | ~3–4 с |
| `_сегм30м.раз` (1 эпоха × 8 сцен) | 57,5 с | 50,9 с (−11%) | ~2 с/сцена |
| Внутренний цикл conv | ~57 инстр. | ~37 инстр. | ~10 инстр. |

Замер после каждого этапа; регрессии откатываются. Метрики
нейросети после всех этапов А+Б1 побайтно идентичны базе
(BCE 0.086, IoU0 0.897, mIoU 0.299 на зерне 424242).

## Этап А — выбор инструкций

Локальные правки `raz/ген.раз`, каждая проверяется на `.иск`-тексте.

- [x] **A1. Масштабная адресация в `индекс_адрес`** (8dabe61):
  для имён-оснований `АДР [база + АКК*масш + дисп]` без стекового
  пробега; регистровый указатель — прямая база SIB.
- [x] **A2. `УМН` на константу** (d78673a): степень двух → `СДЛ`,
  прочее — `УМН АКК,АКК,imm`; `*0`/`×1` свёрнуты; имм только при
  `_в_имм32` (x86-64 sign-extend).
- [x] **A3. Прямой jcc в булевом контексте** (d655889):
  `усл_прыг` — сравнения и `и`/`или`/`не` эмитят `СРАВ + jcc`;
  `_сегм30м`: УСТcc/БЕЗРАСШ 368 → 206.
- [x] **A4. Сравнение с нулём и константой** (d78673a):
  `СРАВ АКК, imm` напрямую.
- [x] **A5. xmm m128-источник** (e5ed07e + правка 4dfa3f4):
  коммутативные векторные операции читают спиленный левый операнд
  как `m128` без ПЛАВ1-перетасовки. **Найденный дефект**: `movups`
  терпит невыравненность, а `addps xmm,m128` требует 16-байтовой
  границы — при нечётной `ген.глуб` спилл резервирует 24 байта.
- [x] **A6. Прямая запись в слот** (80d0af2): `присв_слот` —
  присваивания по имени локала/параметра прямо по `[УКК±смещ]`,
  без `АДР+ВСТЕК+ИЗСТЕКА`. Скаляры, вещи и векторы.
- [x] **A7. `/` и `%` на степень двух** (45f715c): сдвиги и
  bias-компенсация усечения вместо `idiv` (~30+ тактов).
- [x] **A8. Диапазон `a >= 0 и a < K`** (13a3c11): одна
  беззнаковая проверка `без(a) < K`; K только константа.

## Этап Б — пакетные интринсики

Экспонировать существующие инструкции ИСКРЫ в языке.

- [x] **Б1. `макс(век,век)` / `мин(век,век)`** (4dfa3f4):
  `МАКСПС`/`МИНПС`/`МАКСПД`/`МИНПД` с m128-источником;
  `релу4` = `макс(в, повтор(0.0))`.
- [ ] **Б2. Запись в лейн `в[i] = x`** (`ПИНСР*`/extract):
  argmax в `гр_макс_пул2`, scatter-паттерны.
- [ ] **Б3. Пакетное сравнение → маска** (`СРАВПС`) и
  `выбери(маска, а, б)` через `ПИ`/`ПИН`/`ПИЛИН`: blend без
  ветвлений; `релу_гр4`, клиппинг `сигм4` без скалярных веток.
- [ ] **Б4. Целочисленные маски** `век цел.* х N` для карт
  перекрытий: `ПСРАВ`/`ПИ`/`ПИЛИ` вместо скалярных циклов.

## Этап В — цикловые оптимизации

Главный множитель; требует анализа тела `для` перед генерацией.

Подготовка (ручной вынос в `_сегм30м.раз`, прецедент для В2):
во всех горячих функциях (`свертка_1_32`, `свертка_пак`,
`макс_пул2`, `вверх2`, `соедини`, `выход_масок` и все `гр_*`)
основания адресов вынесены на уровень инвариантности —
`ряд*ширина`, `пиксель*блоков`, `часть*вб*4*выб`, весовые
индексы раскрыты как `осн + счётчик*шаг`. В `гр_макс_пул2`
четыре вектора-соседа загружаются один раз, а не по лейну.
Полный прогон 6×24: ~23 мин → 14м19с суммарно с этапами А/Б,
вывод побайтно идентичен. Компиляторный В2 должен воспроизвести
эти же выносы автоматически.

- [x] **В1. Индукционные переменные** (ген.раз): для `для` по
  диапазону каждый `указатель[А + сч*Б]` по сырому `*Т` получает
  бегущий адрес в скрытом слоте — вся адресная арифметика на
  инициализации, в теле `ПЕР АКК,[слот]` + одно `ДОБ` на шаге.
  Только сырые указатели (без проверок границ подмена безопасна);
  инвариантность — без мутаций, вызовов, обращений к памяти и
  вложенных счётчиков. Побочно покрыт В2 для адресов: чисто
  инвариантный индекс получает слот без шага.
- [ ] **В2. Вынос инвариантов (LICM) значений**: чистые
  выражения без зависимости от счётчика — перед циклом
  (адреса уже покрыты В1; остаются скалярные вычисления).
- [ ] **В3. Свёртка `инд & (N-1)`/`инд % K`** уже доказуема для
  границ — распространить на адресную арифметику.

## Этап Г — структура генератора

Архитектурные PR по правилам AGENTS.md, отдельно от этапов А–В.

- [ ] **Г1. IR между AST и текстом ИСКРЫ**: трёхадресный линейный
  IR; пипхол по тексту остаётся финальной полировкой. Имена меток
  перестают быть носителем семантики.
- [ ] **Г2. Инлайнинг малых функций** (`релу4`, `макс4`, векторные
  помощники) — сейчас настоящие вызовы внутри циклов.
- [ ] **Г3. Распределение регистров**: линейное сканирование
  интервалов вместо 5 локалей по счётчику упоминаний.
- [ ] **Г4. Параметризация по размеру** (comptime-каналы): убрать
  дублирование `свертка_*`/`гр_*` под каждую форму тензора.

## Этап Д — широкие векторы (ИСКРА)

Отдельная веха ассемблера, только после этапов А–В: иначе широкие
регистры кормятся тем же адресным оверхедом.

- [ ] **Д1. VEX-префиксы и ymm** (`век вещ.рост х 8`, AVX2):
  удвоение ширины conv/матмул-ядер.
- [ ] **Д2. FMA** (`СЛИЯНУМН*`) — одна инструкция на
  `накоп += a * b`, горячую форму всех ядер.
- [ ] **Д3. Определение возможностей ЦП** (`ЦПУИД`) и выбор
  SSE2/AVX2-пути на старте или `--машина sse2|avx2`.

## Правила этапов

- Каждый пункт — отдельный коммит; этап — минимум один PR.
- После каждого пункта: фикс-точка самосборки (`сам1 == сам2`),
  нативный прогон `тесты/`, замер на `_ш5м` и/или `_сегм30м`.
- Регрессия точности или фикс-точки → откат пункта, запись в
  SETUP_LOG с цифрами.
- Оптимизация не ослабляет проверки `надёжно`: границы проверяются
  до адресной арифметики, как сейчас.
