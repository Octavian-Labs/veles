# ВЕЛЕС — идейник программ и игр

Здесь собраны небольшие проекты, которые одновременно полезны пользователю и
проверяют реальные возможности ИСКРЫ, РАЗУМА, стандартной библиотеки и СЛЕДа.

## Игры

### 1. 2048

Консольное поле 4×4 с управлением `WASD`.

Проверяет:

- двумерную логику поверх массива;
- сдвиг и объединение клеток;
- генератор случайных чисел;
- обработку клавиш;
- сохранение результата;
- новые проверки границ массивов.

### 2. Сапёр

Проверяет:

- поле произвольного размера;
- случайное размещение мин;
- раскрытие пустых областей через стек или очередь;
- работу с соседями клетки и границами массива;
- время и таблицу рекордов.

Хороший стресс-тест режима `надёжно`.

### 3. Змейка

Потребует:

- чтения клавиатуры без Enter;
- таймера;
- очистки и перерисовки консоли;
- неблокирующего ввода;
- очереди сегментов змеи.

Это проверка не только языка, но и консольного ИПП трёх ОС.

## Полезные инструменты

### 4. ОКО — инспектор PE/ELF/Mach-O

Пример:

```text
око программа.exe
```

Показывает:

- формат и машину;
- базу образа и точку входа;
- секции, размеры и права;
- импорты и экспорты;
- hex-дамп выбранной секции.

ОКО пригодится для проверки файлов ИСКРЫ, диагностики эмиттеров и совместной
работы со СЛЕДом. В будущем сможет строить и проверять карты символов.

### 5. СРАВНИ — двоичное сравнение файлов

Пример:

```text
сравни первое.exe второе.exe
```

Показывает:

- размеры файлов;
- первое отличие;
- изменившиеся диапазоны;
- процент совпадения;
- посекционное сравнение исполняемых файлов.

Пригодится для фикс-точки самосборки, анализа оптимизаций и CI.

### 6. ИЩИ — поиск текста по файлам

Пример:

```text
ищи "точка_пост" след
```

Планируемые возможности:

- поиск UTF-8;
- номера строк и столбцов;
- рекурсивный обход каталогов;
- несколько путей;
- фильтр расширений;
- регистрозависимый и независимый режимы;
- вывод контекста;
- коды возврата для сценариев и CI;
- понятные русские ошибки.

Это первый проект из идейника, взятый в разработку.

### 7. ПРОВЕРЬ — контрольные суммы

Пример:

```text
проверь файл.exe
```

Первый этап — CRC32, затем SHA-256. Инструмент пригодится для сид-бинарников,
проверки загрузок и фикс-точки.

## Рекомендуемый порядок

1. **ИЩИ** — повседневная утилита и развитие файлового ИПП.
2. **2048** — наглядная проверка языка и безопасных массивов.
3. **ОКО** — серьёзный инструмент экосистемы и помощник СЛЕДа.
4. **СРАВНИ** — компактная утилита для самосборки и CI.
5. **Сапёр** — стресс-тест границ и обхода поля.
6. **ПРОВЕРЬ** — контроль сидов и выпусков.
7. **Змейка** — после появления полноценного неблокирующего консольного ИПП.
