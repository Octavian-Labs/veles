# ВЕЛЕС — текущие задачи

Дата сверки: 2026-09-21

Этот файл отвечает на вопрос «что делать следующим». Долгосрочные направления
живут в [ROADMAP.md](ROADMAP.md), канонические решения — в
[docs/05-канон.md](docs/05-канон.md), правила быстрого входа — в
[AGENTS.md](AGENTS.md). Задачи СЛЕДа ведутся отдельно в
[след/ЗАДАЧИ.md](след/ЗАДАЧИ.md).

## Текущая точка

- [x] Русские понятные имена во всей кодовой базе.
- [x] Каноническое объявление `введи имя = выражение`; `:=` оставлено только
  как переходная форма с предупреждением.
- [x] Windows PE64, Linux ELF64 и macOS Mach-O x86-64.
- [x] Самосборка РАЗУМА и ИСКРЫ, фикс-точка на трёх ОС.
- [x] Строгий `лог` — PR #32/#33, коммит `6096348`, итог `main` `3dd5971`.
- [x] Матрица строгого `лог`, нормализация значений и нативный прогон 24/24.
- [x] Ось безопасности `--режим надёжно|быстро` — ветка
  `фича/режим-безопасности`: `надёжно` по умолчанию, режим в `Ген`, прагма
  `;режим` в ИСКРЕ, русская ошибка на неизвестное значение, CI трёх ОС.
- [x] Границы массивов `Т[N]` — ветка `фича/границы-массивов`: статическая
  ошибка в обоих режимах, динамическая проверка в `надёжно` (знаковый `< 0`,
  беззнаковый сверху), сбой → `стд_сбой_границы` с кодом 101.
- [x] Границы срезов `[]Т` — ветка `фича/границы-срезов`: индексация по длине
  среза (101), создание проверяет порядок и верхнюю границу до вычисления
  длины (102), `..=` ловит `кон >= длина`, пустые срезы допустимы, срез
  сырого указателя предупреждает.
- [x] Границы строк `стр` — ветка `фича/границы-строк`: байтовая индексация
  `стр[индекс]` по длине UTF-8 (101), индекс `длина` и пустая строка
  запрещены, `стр_срез` проверяет диапазон всегда (102) — обхода синтаксиса
  нет.

## Выполненный этап — `фича/типы-размер-смещение`

Цель этапа: типы `размер`/`смещение`, ABI-совместимые на x86-64 с
`нат.верх`/`цел.верх`. Выполнено:

- [x] Парсер принимает `размер`/`смещение` как типы без уровня:
  внутренне `без64`/`цел64` — ABI и кодогенератор не изменены,
  фикс-точка самосборки сохранена.
- [x] `размер` ↔ `нат.верх` и `смещение` ↔ `цел.верх` взаимозаменимы;
  `размер` и `смещение` между собой несовместимы без явного `как` —
  знак не переносится неявно (кейсы-ошибки в прогоне).
- [x] Стандартная библиотека мигрирована семантически: `память`,
  `список`, `буфер`, `стр` (`стр_найди` → `смещение`), `узлы`,
  `сист.файл`, `система`, `консоль`, `процесс`. Значения, хендлы,
  дескрипторы и коды API остались `нат.верх`/`цел.верх`.
- [x] Правило выбора типов зафиксировано в каноне §6; подсветка VS Code
  обновлена; CI-шаг «Типы — размер/смещение» в трёх джобах.
- [x] Прогон 73/73, фикс-точка, сиды трёх ОС и СЛЕД пересобраны.

Осталось следующими PR: миграция `raz/`, `иск/`, `след/` и сохранение
исходного имени типа в диагностике (сейчас печатается `нат.верх`/
`цел.верх`).

## Следующий PR — `фича/размер-смещение-ядро`

Цель PR: семантическая миграция `нат.верх`/`цел.верх` на
`размер`/`смещение` в `raz/` и `иск/` — по правилу канона §6 (размерные
величины → `размер`, знаковые смещения и «позиция или -1» → `смещение`,
значения/хендлы/коды/битовые поля форматов остаются `нат.верх`/
`цел.верх`). Механической замены не делать: `нат.верх` в `иск/` —
прежде всего фиксированные поля PE/ELF64/Mach-O.

## Выполненный этап — `фича/диагностика-памяти`

Требования этапа выполнены:

- [x] Ось `--память обычно|проверка` независима от `-O0/-O1` и `--режим`;
  `обычно` по умолчанию, неизвестное значение — русская ошибка CLI;
  прагма `;память проверка` в ИСКРЕ.
- [x] Заголовок блока 48 байт (размер, магия `0x51C0DE51`, строка
  выделения, связь списка, состояние) и огранка 16 байт `0x5A`; узлы
  связаны списком от `стд_пам_голова`; новая область заливается `0xCD`.
- [x] `освободи` обходит список: чужой указатель — вид 0, повторное
  освобождение — вид 1, порча огранки — вид 2, порча магии — вид 3;
  область заливается `0xDD`, узел остаётся в карантине.
- [x] `стд_сбой_целост` — стабильный код 104, диагностика в stderr без
  `консоль`, аварийный путь без выделения памяти.
- [x] `стд_пам_фин` перед выходом проверяет живые узлы и печатает отчёт
  об утечках (`N блоков, M байт`) → 104; чистый выход сохраняет код
  программы.
- [x] Помощники испускаются только по битам `нужны_стд` 32/64; в `обычно`
  и без операций памяти ИСКРА не меняется.
- [x] Ограничения зафиксированы в каноне: запись в освобождённый блок не
  перехватывается, прямой `система.выход` обходит финальную проверку.
- [x] CI проверяет коды 42/104 и диагностику stderr на трёх ОС; прогон
  69/69, фикс-точка побайтно, сиды пересобраны.

## Выполненный этап — `фича/оптимизация-проверок`

Требования этапа выполнены:

- [x] Константный индекс в границах не получает динамическую проверку
  (свёртка `ген.сверни`, было до этапа); вне границ — статическая ошибка
  в обоих режимах.
- [x] Доказательства в `ген.индекс_доказан`: `инд & (N-1)` и `инд % K`
  с беззнаковым индексом, переменная `для и в мин..предел` с константными
  границами в пределах массива, переменная `для и в кол` для той же
  коллекции (включая загрузку элемента `для и, э в кол`).
- [x] Доказательство снимается при мутации переменной цикла или самой
  коллекции в теле, при затенении имени и при передаче адреса — сканер
  `_мутирует`/`тело_мутирует` обходит всё дерево тела.
- [x] Недоказанные случаи сохраняют проверку и завершаются кодом 101:
  предел за границей массива, знаковый `%`, чужая коллекция, мутации.
- [x] Доказательство работает на уровне генерации — результат одинаков
  для `-O0` и `-O1`; пипхол-оптимизатор проверок не трогает.
- [x] CI проверяет отсутствие `стд_сбой_границы` в доказанном случае и его
  сохранение при мутации на всех трёх ОС; прогон 61/61.

## Выполненный этап — `фича/сбой-границы`

Требования этапа выполнены:

- [x] Автоматически доступны `стд_сбой_границы`, `стд_сбой_среза` и
  `стд_сбой_памяти` — испускаются по битам `нужны_стд` 4/8/16.
- [x] Стабильные коды: 101 — граница, 102 — срез, 103 — память.
- [x] Минимальная диагностика в stderr без `консоль`: `стд_печ_стр` и
  `стд_печ_чис` через `WriteFile`/`write(2)`; формат
  `разум: сбой границы: индекс -3 вне длины 4, вид 1, строка 5)`.
- [x] ABI: `СЧТ`=значение, `ДАН`=предел, `ИСТ`=вид (0/1/2/3/4),
  `БАЗ`=строка, `Р8`=файл (резерв, 0).
- [x] Windows, Linux, macOS: аварийный путь не выделяет память — фрагменты
  в секции данных, буфер числа в кадре обработчика.
- [x] `выдели`/`новый` при неудаче уходят в код 103 без записи по нулевому
  указателю; сайты ставят `БАЗ`=номер строки.
- [x] СЛЕД останавливается на `стд_сбой_границы` по имени символа
  (пролог + `НОП`-терминатор невозвратного пути); сценарий в Windows CI.
- [x] CI проверяет stderr и коды 101/103 на всех трёх ОС.

## Выполненный этап — `фича/границы-строк`

Требования этапа выполнены:

- [x] `стр[индекс]` проверяется по байтам UTF-8: `0 <= индекс < длина`.
- [x] Индекс `длина` запрещён, пустая строка покрыта тестом (`ст_пустая`).
- [x] Срез строки `стр[а..б]` использует те же проверки, что срез
  массива/среза (длина из `{данные, длина}`).
- [x] `стр_срез` проверяет `от <= по <= длина` всегда и завершает процесс
  кодом 102 — небезопасного обхода синтаксиса нет. Для этого `биб/стр.раз`
  берёт `система`; цикл `стр ↔ система` отсекается загрузчиком по пути.
- [x] CI отдельно проверяет оба режима на Windows, Linux и macOS.
- [x] Обновлены `README.md`, `docs/04-разум.md`, `docs/05-канон.md`,
  `ROADMAP.md`, этот файл и `SETUP_LOG.md`.

## Выполненный этап — `фича/режим-безопасности`

Требования этапа выполнены:

- [x] CLI принимает `--режим надёжно` и `--режим быстро`.
- [x] Без флага выбирается `надёжно`.
- [x] Неизвестное значение режима даёт русскую ошибку и ненулевой код (2).
- [x] Режим хранится в состоянии генератора (`Ген.режим`) и доступен
  последующим этапам.
- [x] `-O0/-O1` не меняет режим безопасности.
- [x] Строгий `лог` одинаков в обоих режимах.
- [x] Прагма `;режим надёжно|быстро` в начале ИСКРЫ различает сборки
  режимов; формат кэша не вводился.
- [x] CI отдельно компилирует положительные и отрицательные случаи обоих
  режимов на Windows, Linux и macOS.
- [x] Обновлены `README.md`, `docs/04-разум.md`, `docs/05-канон.md`,
  `ROADMAP.md`, этот файл и `SETUP_LOG.md`.

## Очередь базовой безопасности

Каждый пункт — отдельная ветка и PR. Порядок менять только осознанным решением
с обновлением канона и ROADMAP.

### 1. `фича/режим-безопасности`

Состояние: **выполнено** локально в ветке `фича/режим-безопасности`
(проверки: прогон 24/24, фикс-точка, байт-идентичная ИСКРА, сборка СЛЕДа).

### 2. `фича/границы-массивов`

Состояние: **выполнено** локально в ветке `фича/границы-массивов`
(проверки: прогон 32/32, фикс-точка, байт-идентичная ИСКРА, сборка СЛЕДа).

### 3. `фича/границы-срезов`

Состояние: **выполнено** локально в ветке `фича/границы-срезов`
(проверки: прогон 42/42, фикс-точка, байт-идентичная ИСКРА, сборка СЛЕДа).

### 4. `фича/границы-строк`

Состояние: **выполнено** локально в ветке `фича/границы-строк`
(проверки: прогон 50/50, фикс-точка, сборка СЛЕДа, сиды трёх ОС
пересобраны — изменилась `биб/стр.раз`).

### 5. `фича/сбой-границы`

Состояние: **выполнено** локально в ветке `фича/сбой-границы`
(проверки: прогон 51/51, фикс-точка, сборка СЛЕДа, сиды трёх ОС
пересобраны; диагностика stderr и точка СЛЕДа на обработчике подтверждены).

### 6. `фича/оптимизация-проверок`

Состояние: **выполнено** локально в ветке `фича/оптимизация-проверок`
(проверки: прогон 61/61, фикс-точка, сборка СЛЕДа, сиды трёх ОС
пересобраны; доказанные проверки отсутствуют в ИСКРЕ, недоказанные дают 101).

### 7. `фича/диагностика-памяти`

Состояние: **выполнено** локально в ветке `фича/диагностика-памяти`
(проверки: прогон 69/69, фикс-точка, сиды трёх ОС пересобраны; `--память
проверка` ловит двойное/неизвестное освобождение, порчу огранки и магии,
утечки — код 104). Владение `стр` и будущей `Строка` остаётся отдельным
проектным решением; borrow checker в генератор не вводился.

### 8. `фича/типы-размер-смещение`

- [x] Добавить `размер` и `смещение`, ABI-совместимые на x86-64 с
  `нат.верх`/`цел.верх`.
- [x] Устранить противоречие фиксированного и машинного смысла `верх`.
- [x] Мигрировать стандартную библиотеку; компилятор и ИСКРУ —
  следующим PR (`фича/размер-смещение-ядро`).
- [x] Не выполнять массовую смену одним коммитом.

### 9. `фича/интерфейсы-модулей`

- [ ] Явные `открыто`/`скрыто`, по умолчанию `скрыто`.
- [ ] Запрет доступа к скрытым именам из другого модуля.
- [ ] Интерфейс `.рми`: версии, цель, режим безопасности, ABI, экспорты,
  раскладки, зависимости и хеши.
- [ ] Повреждённый/чужой кэш молча перестраивается.
- [ ] Семантика `взять сист.файл` и квалифицированных имён сохраняется.

## Обязательная проверка каждого этапа

- [ ] Новый отрицательный тест сначала действительно падает.
- [ ] Нативный прогон проходит на Windows.
- [ ] РАЗУМ компилирует сам себя; два поколения дают идентичную ИСКРУ.
- [ ] ИСКРА собирается новым РАЗУМОМ.
- [ ] СЛЕД компилируется.
- [ ] Сиды PE64/ELF64/Mach-O обновлены, если изменился код компилятора.
- [ ] CI Windows/Linux/macOS зелёный.
- [ ] Документация не выдаёт будущую возможность за реализованную.

## Параллельные направления

Они не должны незаметно вытеснять текущий цикл базовой безопасности:

- версии канона и инструмент `переведи`;
- `вещ.*` (включая `вещ.шаг`/`вещ.шагм`), `век` и SIMD;
- отладочные метаданные `--отладка` вместо рантайм-рефлексии;
- ОС на ВЕЛЕСЕ;
- атомы и параллелизм;
- AArch64/RISC-V;
- приборы и квантовые схемы;
- ЯР;
- развитие СЛЕДа по [след/ЗАДАЧИ.md](след/ЗАДАЧИ.md).
