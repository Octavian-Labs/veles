# Журнал установки и сборки (SETUP_LOG)

## 2026-09-19 — Этап 1 (MVP): ассемблер ИСКРА + PE64 «Привет, мир!»

**Цель:** русский ассемблер → реальный x86-64 машинный код → PE64 .exe.

**Выполнено:**
- Каркас репозитория: `git init`, ветки develop + main + feature/*.
- Ассемблер на Python 3.14: лексер → парсер → кодировщик x86-64
  (REX, ModRM/SIB, RIP-относительная адресация) → эмиттер PE64
  (таблица импортов kernel32.dll).
- Эмиттер ELF64 (Linux, syscall) — та же кодовая база кодировщика.
- Пример `привет.иск` собран в `привет.exe` (1536 байт), запуск:
  выводит «Привет, мир!», код выхода 0.
- Пример `привет_linux.иск` собран в ELF64 (179 байт); запуск проверяется
  в CI на ubuntu-latest (локально WSL отсутствует).

**Найденные и исправленные дефекты:**
1. `ВЫЗОВ импорта` (6 байт) и `ВЫЗОВ метки` (5 байт) — разная длина:
   импорты собираются предсканированием до первого прохода.
2. Карта IAT хранила RVA вместо VA — исправлено на ImageBase + RVA.
3. `ПЕР рег, символ` не вычислял значение на втором проходе (был 0).
4. `РАВНО` конфликтовало: константа vs переход je — разрешено позицией.
5. `mov r32, -11` — негативные значения: введена «гибкая» проверка диапазона.

## 2026-09-19 — Ребрендинг: РЯП/РАСМ → ВЕЛЕС/ИСКРА/РАЗУМ/СОЗНАНИЕ

**Цель:** современные русские имена для экосистемы.

**Выполнено:**
- Проект: `ryap` → `veles` (папка, pyproject).
- Ассемблер: РАСМ → **ИСКРА** (пакет `asm_rus` → `iskra`, CLI `iskra`).
- Системный язык: СИ-РУС → **РАЗУМ** (этап 3, в планах).
- Объектный язык: СИ++-РУС → **СОЗНАНИЕ** (этап 4, в планах).
- Расширение исходников: `.асм` → `.иск`.
- Обновлены README, docs, тесты, CI.

**Команды сборки:**
```powershell
cd src; python -m iskra ..\examples\привет.иск -o ..\examples\привет.exe
```

## 2026-09-19 — Спецификация ЯР 2.0 (EBNF)

**Цель:** формальная грамматика флагманского языка экосистемы.

**Выполнено:**
- `docs/03-яр.md`: полная EBNF-грамматика (лексика, операторы, выражения,
  приоритеты), таблица истинности, ключевые слова (~30).
- ЯР заменил СОЗНАНИЕ в стеке: ИСКРА → РАЗУМ → ЯР.
- Закрыты двусмысленности: `пусто` (значение/предикат по позиции),
  `оно` (результат последнего оператора), именованные аргументы (`=`),
  диапазоны (`..` полуоткрытый, `..=` включая), склонения `раз/раза/разы`.

## 2026-09-19 — Спецификация РАЗУМ (EBNF)

**Цель:** системный язык среднего уровня (статика, ручная память, без GC).

**Выполнено:**
- `docs/04-разум.md`: типы (цел8..64/без8..64/стр/указатели/срезы),
  функции и методы (`функ (т Точка) норма()`), `отложи` (defer),
  `внеш` для FFI, блок `искра:` — встроенный ассемблер.
- Архитектура компилятора: РАЗУМ → текст .иск → iskra.assemble() → PE/ELF.
- Стратегия: прототип компилятора на Python → самораскрутка на РАЗУМЕ.

## 2026-09-19 — Компилятор РАЗУМ v0.1 (MVP): .раз → .иск → exe

**Цель:** рабочий компилятор РАЗУМА поверх ИСКРЫ; первые запускаемые
программы на РАЗУМЕ.

**Выполнено:**
- Пакет `src/razum`: лексер (отступы ОТСТУП/СНЯТИЕ, комментарии `#`/`;`),
  парсер (рекурсивный спуск, группы параметров как в Go: `а, б цел64`),
  генератор текста ИСКРЫ.
- Кодген: кадр через УКК, Win64 ABI (СЧТ/ДАН/Р8/Р9 + тень 32 байта) и
  SysV для linux-цели; значения выражений — в АКК, промежуточные —
  ВСТЕК/ИЗСТЕКА.
- Возможности MVP: функции и вызовы (именованные аргументы), `внеш`,
  объявления `:=` и `имя тип`, типы цел/без/бул/сим/стр/указ/масс/срез/
  запись/переч, `если`/`а если`/`иначе`, `пока`, `вечно`, `для` по
  диапазону и массиву, `выбор`, `верни`, `хватит`, `следующий`,
  `отложи`, `искра:`-блоки, `новый`/`освободи` (куча с заголовком),
  `как`, `размер`, `&`/`*`, `в a..b`, строки → секция данных.
- В ИСКРЕ добавлены `УСТ<усл>` (setcc), `ЗНАКРАСШ`/`БЕЗРАСШ` (movsx/movzx),
  `ЗНАКРАСШД` (movsxd).
- Проверено запуском: `привет.раз` → exe печатает «Привет, мир!» (код 0);
  `вычисления.раз` → `сумма_до(10)` → код выхода 55;
  `выход_linux.раз` → ELF64, код 42.
- CI: windows-джоба прогоняет оба примера РАЗУМА, linux-джоба —
  собранный ELF с кодом 42.
- Тесты: 24 новых (лексер/парсер/генерация/e2e) — итого 63 зелёных.

**Автопроверки:** `black --check` ✓ `flake8` ✓ `pytest` 63 passed.


## 2026-09-19 — РАЗУМ v0.2: закрытие пробелов языка

**Цель:** добить пробелы языка до stdlib «основа» и самораскрутки.

**Выполнено:**
- **Методы** `функ (т Точка) имя(...)` / `функ (т *Точка) имя(...)`;
  вызов `об.имя(...)`, приёмник — первым аргументом как `*запись`.
  Метки функций-методов: `ф_Тип_имя`.
- **Косвенные вызовы**: переменные/параметры типа `функ(...)` хранят
  сигнатуру; вызов идёт через `ВЫЗОВ Р11` (ИСКРА: `ВЫЗОВ рег/память` уже
  поддерживался, FF /2).
- **Литералы** `Тип{...}` для записей (`Точка{х: 3, у: 4}`, именованные
  и позиционные) и массивов (`без8[5]{...}`); скрытый слот обнуляется,
  типы и диапазоны литералов проверяются.
- **Срезы** `об[a..b]` / `a..=b` для массивов/указателей/срезов/стр —
  результат пара {данные, длина} в скрытом слоте.
- **`взять`**: загрузчик модулей (`src/razum/loader.py`) — `имя.раз`
  рядом с источником или в `lib/`, циклы отсекаются по каноническому
  пути. CLI теперь работает через загрузчик.
- **`искра:` видит локали**: имена переменных/параметров подставляются
  как `[УКК±off]`; комментарий `#` внутри блока допустим.
- **Проверка типов**: `_совместим` при объявлении/присваивании и в
  литералах — литералы проверяются по диапазону целевого типа,
  остальное — по виду типа.
- **Исправлен коренной баг кадра**: слот размера sz теперь занимает
  [УКК-(off+sz-8) .. УКК-off+7] — раньше записи/массивы-локали
  перекрывали соседние слоты и сохранённый УКК.
- Парсер: `функ`/`[`/`функ(` распознаются как начало типа в объявлении;
  `Тип{...}` в постфиксной позиции; `об[a..b]` — СрезДиап.
- Пример `examples/пробелы.раз` собирается и возвращает 52
  (методы 57 + косвенный вызов 2 + срез 3+20 − 30 через искра).
- Тесты: +14 (парсер/ген/модули/e2e) — итого 77 зелёных.

**Автопроверки:** `black --check` ✓ `flake8` ✓ `pytest` 77 passed.

## 2026-09-19 — РАЗУМ v0.3: stdlib «основа»

**Цель:** первая стандартная библиотека на самом РАЗУМЕ — фундамент
самораскрутки.

**Выполнено:**
- `lib/память.раз`: `заполни`, `скопируй`, `равна_память` — на чистом
  РАЗУМЕ, без внешних вызовов.
- `lib/стр.раз`: `стр_собери`, `стр_равны`, `стр_срез`, `стр_склей`,
  `цел_в_стр` (десятичный вывод со знаком). Разметка стр собирается
  через служебную запись `_стрпара` + `как стр`.
- `lib/консоль.windows.раз`: `печать`/`печатьстр`/`печатьцел` через
  WinAPI (`GetStdHandle` + `WriteFile`).
- `lib/консоль.linux.раз`: то же через `искра:`-блок с syscall write —
  блок искры обращается к параметру `с` по имени (подстановка [УКК+off]).
- `lib/основа.раз` — зонтик: `взять основа` подтягивает всё.
- Загрузчик: платформенный файл `имя.<цель>.раз` имеет приоритет;
  поиск — рядом с источником, в `lib/` рядом и уровнем выше.
- Компилятор: встроенная `выдели(n)` (размер во время исполнения →
  `*без8`), дедуп импортов `внеш` при повторных объявлениях.
- Исправлен парсер: `имя[i] = x` снова индексация, а `имя []Т` — срез-тип.
- `examples/основа_демо.раз`: реальный вывод «Привет из основы! /
  Ответ: 42 / склейка работает / стр_равны: да», код выхода 42;
  та же программа собирается в ELF64 с консоль.linux.раз.

**Автопроверки:** `black --check` ✓ `flake8` ✓ `pytest` 79 passed.

## РАЗУМ v0.4 — stdlib: файлы, ввод, система, список

- `lib/система.{windows,linux}.раз`: `выход(код)` — ExitProcess / sys_exit(60).
- `lib/файл.windows.раз`: `файл_прочти`/`файл_запиши` через CreateFileA,
  GetFileSize, ReadFile, WriteFile, CloseHandle.
- `lib/файл.linux.раз`: то же через `искра:` — syscalls open(2), lseek(8),
  read(0), write(1), close(3); имена параметров подставляются в `[УКК+off]`.
- `lib/консоль.*`: `ввод()` — читает весь stdin до EOF (буфер 1 МиБ).
- `lib/стр.раз`: `стр_в_нуль` (ASCIIZ для WinAPI), `стр_найди`, `стр_число`
  (десятичный разбор со знаком), предикаты `это_цифра`/`это_буква`/
  `это_пробел`/`это_знак`.
- `lib/список.раз`: запись `Список` + `список_новый`/`список_добавь`/
  `список_получи`/`список_свободен` — динамический массив без64 с ростом ×2.
- Исправления компилятора:
  - параметры функции регистрируются до вывода типов `:=` в теле
    (`б := с.поле[i]` раньше падал «неизвестное имя»);
  - метка короткого замыкания `или` не содержит пробела;
  - переменная не может называться `размер` (конфликт с `размер()`);
  - указатели проверяются по базовому типу: `*Точка` не присваивается
    `*Другое`; `*пусто` остаётся универсальным.
- Примеры: `файл_демо.раз` (запись→чтение→поиск→число, код 42),
  `список_демо.раз` (код 60), `выход_демо.раз` (код 7),
  `ввод_демо.раз` (stdin → длина).

**Автопроверки:** `black --check` ✓ `flake8` ✓ `pytest` 84 passed, 1 skipped.

## 2026-09-19 — Расширение VSCode: подсветка ИСКРА/РАЗУМ/ЯР

**Цель:** TextMate-грамматики и манифест расширения `veles-languages`.

**Выполнено:**
- `editors/vscode/`: package.json (publisher veles, v0.1.0), два
  language-configuration (`#` для .раз/.яр, `;` для .иск), три грамматики
  `syntaxes/*.tmLanguage.json` (scopeName source.iskra/razum/yar), README.
- Лексика извлечена из кода: мнемоники/регистры — `src/iskra/encoder.py`,
  `regs.py`; директивы — `parser.py`; ключевые слова и типы РАЗУМА —
  `src/razum/parser.py`, `nodes.py`; ЯР — по черновику `docs/03-яр.md`
  (грамматика помечена предварительной).
- Все JSON проверены `python -m json.tool`; регулярки компилируются;
  смоук-тест на `examples/привет.иск` и `привет.раз` — все строки покрыты.
- Файла LICENSE в корне нет — поле license в package.json не заполнено.

## 2026-09-20 — ИСКРА переписана на РАЗУМЕ (порт ассемблера)

**Цель:** убрать Python из ассемблера — `иск/` = порт `src/iskra/` (~2100 строк Python → РАЗУМ).

**Структура порта:**
- `иск/выраж.раз` — узлы выражений (В_ЧИСЛО/В_СИМ/В_ЗДЕСЬ/В_БИН/В_ОТР/В_НЕ/В_ИАТ),
  вычисление через Ктх, `в_большое` (флаг «литерал ≥ 2^63» для Python-семантики
  беззнаковых чисел), `иск_ош`.
- `иск/рег.раз` — таблица регистров: латинские + русские псевдонимы
  (АКК/СЧТ/ДАН/БАЗ/УКС/УКК/ИСТ/ПРМ/Р8–Р15), суффиксы М/П/Б.
- `иск/лексер.раз` — токены ИТ_ИМЯ/ЧИСЛО/СТР/ЗНАК, числа 0x/0b/0o/десятичные,
  флаг `знач2` для литералов с битом 63.
- `иск/парсер.раз` — операторы и операнды (О_РЕГ/О_ПАМ/О_ИММ), адреса
  [база+инд*масш+смещ], директивы СЕКЦИЯ/ИМПОРТ/ВХОД/БАЙТ…/РАВНО.
- `иск/кодер.раз` — кодировка x86-64: REX/ModRM/SIB, RIP-относительная,
  переходы, вызовы через IAT, УСТ, ЗНАКРАСШ/БЕЗРАСШ/ЗНАКРАСШД (~800 строк).
- `иск/сборщик.раз` — двухпроходная сборка: метки, константы РАВНО/ЗДЕСЬ,
  импорты, секции; `проход1`/`проход2` + план эмиттера (разрыв цикла
  сборщик↔эмиттер).
- `иск/пе.раз` — PE32+ (база 0x140000000, .text/.data, ILT/IAT, hint/name).
- `иск/эльф.раз` — ELF64 (база 0x400000, один PT_LOAD RWE, СИСВЫЗОВ).
- `иск/искра.раз` — драйвер stdin→PE/ELF→stdout; цель — прагмой
  «;цель linux» в первой строке (для Python-версии — комментарий).

**Исправленные баги порта:**
- `\xNN`-литералы ≥0x80 попадали в .иск как сырые байты (UTF-8-ошибка) —
  все opcode-строки заменены на `_байтN(...)` в буфер.
- `_ил_число` пропускал первую цифру (`i := k+1` → `k`) — все числа
  были 0/сдвинуты.
- `_гибк` приводил v к без64 → отрицательные imm32 «не помещались»;
  сравнение сделано знаковым, бит≥64 — без проверки.
- `_рм` для регистра возвращал rex_b в младшем байте вместо <<8
  (REX 0x42 вместо 0x41 для Р8+).
- `_к_расш`/`_к_расшд` брали размер из первого операнда (регистра)
  вместо памяти — БАЙТ[...] отклонялся.
- `;цель linux`: длина слова «цель» 8 байт UTF-8, не 4.
- Большие литералы (0xFFFFFFFFFFFFFFFF): Python видит 2^64-1 → B8 imm64,
  цел64 даёт -1 → C7 imm32. Добавлен флаг знач2/в_большое.

**Паритет (байт-в-байт с Python-версией):**
- `examples/привет.иск` → PE64 идентичен.
- `привет_linux.иск` + `;цель linux` → ELF64 идентичен.
- `искра_сген.иск` (28927 строк — вывод самохост. РАЗУМА) → PE64 идентичен.
- Бинарь поколения-2 (`_self_искра.exe`) рабочий: собирает привет.иск.

**Проверки:** `python -m pytest` — 87 passed. Компиляция порта —
`raz/разум.exe < иск/искра.раз` → сборка `искра_сген.иск` Python-ИСКРОЙ.

## 2026-09-20 — Диагностика парсера РАЗУМА (по ревью)

**Цель:** критичные улучшения из ревью — информативные ошибки парсинга.

**Выполнено:**
- `raz/парсер.раз`: `_получено(т)` — описание токена для ошибок
  ('имя'/число/конец строки/файла), `п_ожидал(что, т)` — «ожидалось X,
  получено Y». Применено в жди/жди_имя/жди_вид/конец_строки и в точках
  «ожидался тип», «отступный блок», «объявление», «неожиданный токен».
- Цепочка из 9 операторов присваивания вынесена в `_оп_присв(т)`;
  повторные `п.ток()` заменены локальной `тк`.
- Пример: «ожидалось ':', получено конец строки» вместо «ожидалось ':'».

**Проверки:** разум.exe пересобран сам собой; pytest 87 passed;
паритет .иск с Python-версией сохранён (с точностью до CRLF).

## 2026-09-20 — Столбцы в диагностике РАЗУМА и ИСКРЫ

**Цель:** ошибки вида «строка N:M» по всей цепочке.

**Выполнено:**
- `Токен.столб` в raz/лексер.раз и `ИТок.столб` в иск/лексер.раз —
  байтовая позиция в строке (1-начало), как `col` в src/razum/lexer.py.
- `Узел.столб` в lib/узлы.раз — узлы инструкций/меток/директив ИСКРЫ
  несут колонку мнемоники.
- `разум.лексер`, `разум.парсер`, `искра` печатают «строка N:M: ...».
- Пример: «искра: строка 3:5: УКС/rsp/r12 не может быть индексным
  регистром», «разум.парсер: строка 1:35: ожидалось ':', получено
  конец строки».

**Проверки:** разум.exe и искра.exe пересобраны самими собой;
байтовый паритет PE сохранён; pytest 87 passed.

## 2026-09-20 — Чистка ссылок на Python-реализацию + ввод() без лимита

**Цель:** убрать из .раз-исходников упоминания портов src/*.py.

**Выполнено:**
- 13 файлов raz/ и иск/: комментарии «(порт src/...py)» и «как в Python»
  переписаны на самостоятельные формулировки.
- Найден и исправлен скрытый баг: ввод() в lib/консоль.windows.раз и
  lib/консоль.linux.раз читал stdin одним вызовом в буфер 1 МиБ —
  разум_сген.иск (1.16 МБ) обрезался, самосборка разума падала.
  Теперь чтение циклом до EOF порциями по 64 КиБ в наращиваемый Буфер.

**Проверки:** разум_сген.иск (43078 строк, 1.16 МБ) собирается
искра_новый.exe байт-в-байт с Python-эталоном; разум_новый.exe
компилирует привет.раз; pytest 87 passed.

## 2026-09-20 — Бенчмарк «простые»: РАЗУМ против Python

**Задача:** счёт простых чисел до 2 000 000 перебором делителей
(чистая арифметика, цикл ~2M итераций с внутренним циклом до √n).

**Файлы:** examples/простые.раз, examples/простые.py (одна логика).

**Результат (3 прогона, лучшее):**
- РАЗУМ→ИСКРА→PE64: ~1.30 сек
- Python 3.14:       ~8.42 сек
- Ускорение: ~6.5×

Проверка: обе версии печатают «простых до 2000000: 148933».

## 2026-09-20 — Исправление ABI Linux: стековые параметры и аргументы()

**Проблема:** искра.elf и разум.elf падали с segfault на любом
нетривиальном входе. Две причины:

1. Стековые параметры (7+ аргументов): callee искал их по [УКК+16],
   но после введения тени 48Б они лежат по [УКК+16+48+8*(i-6)].
   Ломал _финиш (7 параметров) и весь кодер. Формула единая:
   рег.парам i -> 16+8*i; стековый -> 16+8*nreg+8*(i-nreg).
   Правки: src/razum/gen.py и raz/ген.раз.
2. аргументы() читала /proc/self/cmdline через lseek(SEEK_END) —
   procfs всегда отдаёт размер 0, функция возвращала пусто и
   --цель linux не был виден. Переписано на read-цикл до EOF.
   Правка: lib/система.linux.раз.

**Проверки (WSL docker-desktop):** искра.elf собирает
привет_linux.иск -> ELF запускается, печатает «Привет, мир!»;
разум.elf --цель linux компилирует выход_linux.раз -> exit 42;
фикс-поинт самосборки на Linux: сам1.иск == сам2.иск (1104893 байт).
Windows-фикс-поинт сошёлся. pytest: 87 passed.

## 2025-09-20 — регистры для горячих локалей (этап 7, ветка feature/reg-locals)

Горячие скалярные локали и параметры (до 5 шт.) размещаются в callee-saved
регистрах БАЗ, Р12–Р15: счёт обращений с весом глубины циклов, запреты на
Адрес()/искра-блоки/агрегаты, пуши в прологе и восстановление в эпилоге.
Зеркально в src/razum/gen.py и raz/ген.раз; _о_рег64 оптимизатора знает БАЗ.
Поле ГСим.рег — без64-индекс регистра (стр-поле хранится указателем и
падало на нулевом — нельзя было хранить имя регистра строкой).

Проверки: pytest 87 passed; фикс-поинт самосборки побайтно на Windows и
Linux (WSL); паритет .иск Python↔нативный идентичен (CRLF/LF);
examples/простые.раз: 148933 простых, медиана 1492→1363 мс (~9% быстрее).

## 2026-XX-XX — нативный тест-раннер, фикс «отложи», удаление Python (feature/depython)

Нативный раннер тестов тесты/прогон.раз: компилирует примеры и кейсы
цепочкой разум->искра, запускает бинарники и сверяет коды выхода/вывод.
Новые модули lib/процесс.windows.раз (CreateProcessW/WaitForSingleObject/
GetExitCodeProcess) и lib/процесс.linux.раз (fork/execve/wait4, всё на
сырых СИСВЫЗОВ-ах). Кейсы рег_стресс.раз (стресс рег-локалей) и
строки_отложи.раз (строки + отложи). На Linux привет пропускается —
он Windows-only (kernel32-импорты).

Найден и исправлен древний баг: «отложи» исполнялся в эпилоге после
записи возврата в АКК и отложенные вызовы затирали результат. Теперь
АКК прячется на стек до отложенных выражений и восстанавливается после
(raz/ген.раз; Python-эталон удалён до коммита фикса — правка жила там
локально, в git попала только raz-версия).

Python удалён целиком: src/, tests/, pyproject.toml, run_checks.ps1,
examples/простые.py; .gitignore, README и docs приведены к нативному
workflow. CI уже нативный — теперь кроме фикс-поинта гоняет прогон.раз.

Проверки: прогон Windows 11/11, Linux 10/10 (привет пропущен), код=0;
фикс-поинт самосборки побайтно на Windows и Linux.

## 2026-XX-XX — канон стиля: docs/05-канон.md + строгое «=», метки, псевдоинструкции

Принят канон стиля (docs/05-канон.md): только русские имена, минимум
знаков, один смысл — одно написание, единственное число для сущностей.

Семантика РАЗУМА (raz/ген.раз): «имя = выр» — только запись в уже
объявленное имя, иначе ошибка «имя не объявлено»; повторное объявление —
ошибка; «:=» остаётся переходной формой, но не может перекрывать
параметры/функции/константы и печатает предупреждение в stderr раз за
компиляцию (lib/консоль.*: печать_ош). Весь существующий код мигрирован
механически (~180 мест): повторное объявление -> присваивание, семантика
не изменилась (области видимости плоские, слот и так был общий).

Метки кодогенератора — канон м_<функция>_<контекст>_<номер>
(м_главная_пока_начало_3, м_стр_число_выход); опт.раз обновлён под новый
формат (гейт _о_цель_ок: «м_» + исключение срезок и/или по подстроке).

ИСКРА: псевдоинструкции ВХОДКАДРА N / ВЫХОДКАДРА (иск/парсер.раз)
разворачиваются в ВСТЕК УКК; ПЕР УКК,УКС; ВЫЧ УКС,N и ПОКИНЬ.

lib/процесс.*: stderr дочернего процесса наследуется от родителя, а не
сливается в выходной файл — предупреждения компилятора больше не
портят генерируемый .иск.

Проверки: фикс-поинт побайтно на Windows и Linux; прогон 11/11 (Win) и
10/10 (Linux); СЛЕД и все примеры собираются новым toolchain; диагностики
«имя не объявлено»/«перекрытие :=»/«повторное объявление» проверены.

## 2025 — точечные типы и точечные модули (ветка feature/типы-модули)

Канон разработчика: цел.*/нат.*/вещ.*, лог, точечные пути «взять сист.файл»,
модульные цепочки в выражениях (мат.цел.нод, сист.файл.прочти).

Реализация:
- raz/парсер.раз: тип() понимает «цел.старт..верх», «нат.старт..верх»,
  «вещ.*», «пик» (нормализация в машинные размеры: цел.верх→цел64 и т.д.);
  «лог» — алиас бул; старые цел64/без64/бул остаются алиасами перехода;
  «взять» принимает точечные пути через запятую; литералы Тип[N]{..}
  и «как» работают с точечными типами (тип_атом).
- raz/загрузчик.раз: точки пути → подкаталоги (сист.файл → lib/сист/файл.<цель>.раз);
  поиск модуля по базам кат, кат/lib, кат/.., кат/../lib; реальный путь
  отделён от нормализованного ключа seen (дедупликация без ложных путей).
- raz/ген.раз: реестр модулей ГМод; резолвер цепочек У_ПОЛЕ/У_ВЫЗОВ:
  сист.файл.прочти → файл.прочти → файл_прочти (префиксные и листовые
  формы); модульные константы тоже резолвятся.
- lib/: мат.раз (зонтик мат.цел+мат.нат), сист/файл.{windows,linux}.раз;
  плоские файл.{windows,linux}.раз — переходные шимы на сист.файл.
- Миграция ~1500 мест по ~54 файлам: цел64→цел.верх и т.п. (скрипт,
  строковые литералы не тронуты); docs/04-разум.md обновлён.
- Попутно: плоская область имени «n2» в парсере переименована.

Проверки: фикс-поинт разума и искры побайтно на Windows и Linux;
прогон 11/11 (Win), 10/10 (Linux); СЛЕД собирается; демо точечных
типов/модулей: мат.цел.нод, сист.файл.прочти — ок.

## биб + ипп (ветка фича/биб)

- Каталог стдлиба lib/ → биб/; загрузчик ищет биб/ первым, lib/ —
  устаревший запасной путь перехода.
- след/апи.раз → след/ипп.раз (ипп = интерфейс прикладного
  программирования); «взять апи» → «взять ипп».
- Доки: stdlib → стдлиб, lib/ → биб/; канон 05 дополнен таблицей
  (биб, ипп) и правилом русских имён веток (фича/, правка/).
- Ветки теперь называются по-русски: фича/биб.

Проверки: фикс-поинт разума и искры побайтно; прогон 11/11 Win,
10/10 Linux; СЛЕД собирается (взять ипп).

## цель macos / Mach-O x86-64 (ветка фича/macos)

- Новый эмиттер иск/махо.раз: статический Mach-O MH_EXECUTE x86_64 —
  LC_SEGMENT_64 __TEXT (RX, включает заголовок) + __DATA (RW) + LC_MAIN;
  динамического компоновщика нет, вход — смещение от базы __TEXT.
- Прагма «;цель macos» в искра.раз; разум принимает --цель macos
  (цель=2), генерирует прагму и сохраняет исходный УКС в гл_аргв
  (на macOS argc/argv лежат на стеке в точке входа, /proc нет).
- биб/*.macos.раз: система (exit 0x2000001, аргументы через гл_аргв),
  консоль (write/read), сист/файл (open/read/write/close/unlink),
  процесс (fork/execve/wait4/dup2). BSD-вызовы: 0x2000000+N,
  ошибка — флаг переноса, нормализуется в -1.
- хост_линукс() переименован в хост_не_вин() (общий предикат
  «не-Windows»); процесс.macos: пути разум_мак/искра_мак.
- Сиды Mach-O: искра_мак, raz/разум_мак (magic CFFAEDFE).
- CI: джоба macos (macos-14, arm64 + Rosetta 2) — прогон тест-раннера
  и фикс-поинт самосборки.

Проверки: Mach-O-заголовок и LC_* разобраны структурно; байты точки
входа — lea/mov сохранения УКС. Прогон 11/11 Win, 10/10 Linux —
регрессий нет.

## 2026-09-20 — канон версий, целей, доменов и параллелизма; ROADMAP

Ветка фича/канон-версии. docs/05-канон.md (ред. 3): новые разделы
11–14 — версии и устаревание (;версия, конвейер deprecated→ошибка→
удаление, инструмент переведи, отметки ;стабильно/;развивается),
модель целей (пара система-машина, ИСКРА как IR, кодеры как модули),
домены памяти и приборы (процесс/прибор/стойкая/дальняя, область как
интерфейс аллокатора, квант как схема-данные), модель параллелизма
(атом, порядки слабый/захват/отпуск/полный, ЗАМОК и ограды в ИСКРЕ).
Создан ROADMAP.md — пройденные вехи, вехи версий/вещ.*/редактора,
большие вехи ОС/параллелизм/машины/приборы/квант/ЯР. README: таблица
целей приведена к факту (macOS работает), ссылки на канон и ROADMAP,
разделы про редактор и развитие без наслоений.

## 2026-09-21 — миграция кодовой базы на канон: «введи», явные типы, имена циклов

Ветка фича/введи-канон. Парсер: новая форма «введи имя = выражение»
(вывод типа, узел помечен знач2=1); предупреждение «:= устарело»
выдаётся только для настоящего :=. Миграция пятью параллельными
потоками: raz/ген.раз (325 замен), остальной raz/ (219), иск/ (293),
биб/ (38), след/+тесты/+examples/ — итого ~875 объявлений «:=»
переведены на явные типы, 3 места — на «введи». Переменные циклов
переименованы по смыслу (i→инд/поз/ном, j→инд2/кон, k→инд/ном/край);
«для и в» исправлено на «для инд в». Строки-распознаватели старого
синтаксиса в компиляторе сохранены — переходный период обслуживается.
Проверки: разум и искра собраны из мигрированных исходников, фикс-точки
сошлись побайтно (искра.exe идентична прежней), прогон 11/11 на
Windows. Сиды пересобраны для всех трёх ОС: разум.exe/разум/разум_мак,
искра.exe/искра/искра_мак. Документы: 04-разум.md (грамматика «введи»,
уровни типов, примеры), грамматика и сниппеты редактора, README
расширения.

## 2026-09-21 — имена по канону: понятные вместо однобуквенных и латинских

Ветка фича/имена-канон. Канон §7 дополнен разделом «Имена должны быть
понятны»: запрет латинских и однобуквенных имён, осмысленные короткие
русские имена, сокращения только общеупотребимые, имена спецификаций
железа (REX, DR6, УКС) допустимы с комментарием. Загрузчик: seen→взято,
decls→объявы, суф→суффикс, _seen_есть→_взято_ли. след/ядро.раз:
приёмник о→отл, поля хпроц→х_проц, хпоток→х_поток, пид→ид_проц,
тид→ид_потока, локали n→пар и пр. Полный обход четырьмя потоками:
ген.раз (~1600 замен), парсер+лексер (~1270), иск/ (все 11 файлов),
след/+биб/+тесты/+examples/. Поля записей: Парсер.i→поз, Парсер.т→токи,
Ген.lbl→мет_сч и пр. В процессе найден и исправлен сломанный слепой
заменой случай (г.→ген. задело арг./прог.) — поэтому проверки подняты
до уровня бинаря: собранный компилятор гоняется на реальном файле.
Проверки: фикс-точка разума побайтно, искра.exe и сиды всех ОС
байт-идентичны (переименования не влияют на кодогенерацию), прогон
11/11. Известный остаток: поля Узел .а/.б/.в/.г — позиционные дети AST,
переименование отдельной вехой.

## 2026-09-21 — базовая безопасность: строгий `лог`

Ветка `фича/строгий-лог`. Условия `если`/`а если`/`пока` и операторы
`не`/`и`/`или` теперь принимают только `лог`; числовые литералы исключены
из совместимости с `лог`, а приведения чисел и указателей `как лог`
запрещены. Диагностика печатает канонические русские имена фактического
и требуемого типа вместо внутренних кодов AST. Чтение и запись `лог`
нормализуются до 0/1, включая чтение сырого байта через `*лог`.

Миграция выявила два прежних допущения: в `иск/лексер.раз` приведению
правого операнда добавлены скобки, чтобы слабый `как` не относился ко
всему условию; в `след/пе.раз` числовое `ок = 1` заменено на `ок = да`.
Добавлена матрица положительных и отрицательных тестов в `тесты/лог/`,
включая запрет присваивания в условии и обход проверки через константную
свёртку. В CI выделена отдельная проверка строгого `лог` на Windows,
Linux и macOS.

Проверки: нативный прогон 24/24 на Windows; `-O0` и `-O1` одинаково
принимают и отклоняют программы; фикс-точка РАЗУМА сошлась; ИСКРА,
пересобранная новым РАЗУМОМ, байт-идентична сиду; СЛЕД компилируется.
Сиды РАЗУМА и ИСКРЫ пересобраны для PE64, ELF64 и Mach-O x86-64.

## 2026-09-21 — единый контекст проекта для новых сессий

Ветка `фича/контекст-проекта`. Созданы `AGENTS.md` как обязательная краткая
памятка и корневой `ЗАДАЧИ.md` как исполнимая очередь независимых PR. В них
зафиксированы назначение ИСКРЫ/РАЗУМА/ЯРА/СЛЕДа, шесть нативных сидов,
самосборка, строгий `лог`, команды проверки, Git-процесс и следующий точный
этап `фича/режим-безопасности`.

README переписан по фактическому состоянию PE64/ELF64/Mach-O и CI трёх ОС.
`docs/01-архитектура.md` больше не считает macOS будущей целью;
`docs/04-разум.md` использует канонические уровни типов, нативную цепочку без
Python и принятое решение по границам вместо открытого вопроса. ROADMAP
ссылается на оперативный задачник. Документы ИСКРЫ и ЯРА уточняют фактический
статус. Задачник СЛЕДа связан с общим планом и дополнен остановкой на будущих
`стд_сбой_границы`/`стд_сбой_среза` и регрессионным сценарием 101/102.

Проверка документации: относительные ссылки разрешаются в существующие файлы,
`git diff --check` чист; код и сиды не изменялись.

## 2026-09-21 — канон: `век`, половинная точность, метаданные вместо рефлексии

Ветка `фича/канон-век-метаданные`. В `docs/05-канон.md` зафиксированы три
дальних решения по мотивам разбора «чего не хватало бы системному языку под
ИИ-нагрузки»: семейство `век <элемент> х N` — вектор фиксированной ширины,
раскладываемый в SIMD-регистры цели (недоступная ширина — ошибка компиляции);
`вещ.шаг` (f16) и `вещ.шагм` (bf16) — половинная точность в имени типа;
подраздел «Метаданные вместо рефлексии» — программа остаётся «слепой»,
а `--отладка` испускает типы/имена/строки отдельной секцией для СЛЕДа и LSP.

ROADMAP: веха «`вещ.*` и SSE» расширена до «`вещ.*`, `век` и SIMD»; вехе
редактора добавлен пункт отладочных метаданных. Корневой `ЗАДАЧИ.md`
обновляет список параллельных направлений.

Код и сиды не изменялись — документационная дельта.

## 2026-09-21 — ИЩИ v0.1: поиск UTF-8 по файлам

Ветка `фича/ищи` создана от актуального `develop`. Добавлен корневой
`Идейник.MD` с очередью небольших игр и полезных инструментов. Первый проект —
нативная утилита `ищи/` на РАЗУМЕ: разбор аргументов Windows и NUL-разделённых
argv Linux/macOS, поиск с номерами строк и Unicode-столбцов, кириллический
регистронезависимый режим, фильтр расширения, вывод только имён, пропуск
двоичных файлов и коды выхода 0/1/2.

Windows-платформа использует `FindFirstFileW`/`FindNextFileW`, конверсию
UTF-16 ↔ UTF-8, рекурсивный обход и не следует по reparse points. Linux и
macOS v0.1 компилируются и поддерживают прямые пути; рекурсивные
`getdents64`/`getdirentries64` оставлены следующим этапом и явно отмечены в
README. Проверочные данные включают кириллицу и вложенный путь с пробелом.

Проверки: PE64 собран и запущен; сценарий `ищи/проверка.ps1` прошёл поиск
UTF-8, рекурсию, режим без рекурсии, путь с пробелом и коды 0/1/2. Из того же
исходника успешно сгенерированы ELF64 и Mach-O x86-64.

## 2026-09-21 — ось безопасности `--режим надёжно|быстро`

Ветка `фича/режим-безопасности` создана от актуального `develop`. До
изменения флаг `--режим` молча игнорировался (воспроизведено: прагмы
`;режим` в выводе не было).

`raz/разум.раз`: CLI распознаёт `--режим надёжно|быстро`; без флага —
`надёжно`; неизвестное или отсутствующее значение завершает компиляцию
ошибкой «разум: неизвестный режим «…» — ожидается надёжно или быстро»
и кодом 2. `raz/ген.раз`: запись `Ген` получила поле `режим нат.верх`
(1=надёжно, 0=быстро), `ген_новый` принимает его третьим параметром;
`генерируй` испускает прагму `;режим надёжно|быстро` в начале ИСКРЫ.
`raz/ген_тест.раз` обновлён под новую сигнатуру.

Проверки Windows: умолчание и `--режим надёжно` → `;режим надёжно`,
`--режим быстро` → `;режим быстро`, `-O1 --режим быстро` не меняет режим;
`--режим криво` и `--режим` без значения → код 2 с русской ошибкой;
строгий `лог` отклоняет `если 5` в обоих режимах. Сиды пересобраны
(`разум.exe`, `разум`, `разум_мак`), фикс-точка сошлась побайтно, ИСКРА,
собранная новым РАЗУМОМ, байт-идентична прежней, СЛЕД собирается, нативный
прогон 24/24. В `ci.yml` добавлен шаг «Режимы безопасности» для трёх ОС
(умолчание, оба значения, `-O1`, негатив и строгий `лог` в `быстро`).

Сами проверки границ не входили: `надёжно` пока равен `быстро` по
наблюдаемой семантике кроме отметки режима.

## 2026-09-21 — границы массивов `Т[N]` в двух режимах

Ветка `фича/границы-массивов` создана от `develop`. Воспроизведены дефекты:
`Т[7]` для массива `Т[4]` компилировался с кодом 0, динамический выход за
границу завершался кодом 0.

`raz/ген.раз`: контекст доступа проброшен через `адрес`/`поле_адрес`/
`индекс_адрес` (0 — чтение, 1 — адрес/запись); `индекс_адрес` теперь:

- статически отклоняет сворачиваемый индекс вне `Т[N]` в обоих режимах —
  отдельная диагностика для чтения, записи и взятия адреса;
- в `надёжно` испускает проверку до умножения на размер элемента: знаковый
  индекс — `МЕНЬШЕ` на `< 0` плюс `ВЫШРАВ` на `>= N`, беззнаковый — только
  `ВЫШРАВ`; сбой → `стд_сбой_границы` (RDI = вид операции), вызов через
  RAX, аварийный выход кодом 101;
- в `быстро` динамическая проверка не генерируется;
- база и индекс вычисляются однократно (RDI/R15).

`стд_сбой_границы` добавлен в `стд_хелперы` для Windows (`ExitProcess`) и
Linux/macOS (`exit`); поле `нужны_стд` расширено.

Новые кейсы `тесты/границы/` (8 файлов): статика — `конст_чтение`,
`конст_запись`, `конст_минус`; динамика — `дин_чтение`, `дин_запись`,
`дин_минус`, `дин_нат` (код 101), `в_границах` (код 42). Прогон 32/32.
Проверено вручную: статические ошибки в обоих режимах («индекс 7 вне
границ массива[4] при чтении/записи»), все динамические сбои — 101,
легальный доступ — 42, в `быстро` ИСКРА не содержит `стд_сбой_границы`.
Сиды `разум.exe`/`разум`/`разум_мак` пересобраны, фикс-точка побайтно,
ИСКРА новым РАЗУМОМ байт-идентична, СЛЕД собирается.

В `ci.yml` добавлен шаг «Границы массивов» для трёх ОС: код 101 в
`надёжно`, статическая ошибка и отсутствие проверки в `быстро`. Обновлены
README, docs/04, docs/05, ROADMAP, ЗАДАЧИ, AGENTS.

Не входили: проверки срезов и строк, аварийные коды как стабильный
интерфейс, изменение представлений данных.

## 2026-09-21 — границы срезов `[]Т` в двух режимах

Ветка `фича/границы-срезов` от `develop`. Воспроизведены дефекты:
`ср[-2]` и `м[0..9]` компилировались молча, `ср[и]` при `и > длина`
завершался кодом 0, `м[0..к]` при `к > N` возвращал мусорную длину.

`raz/ген.раз`:

- `срез_инд_стат` — константный отрицательный знаковый индекс среза:
  статическая ошибка в обоих режимах («индекс -2 вне границ среза при
  чтении»); длина среза статически неизвестна — верхняя граница только
  динамически.
- `индекс_адрес` (ТВ_СРЕЗ): в `надёжно` индекс в СЧТ сверяется с
  `[АКК+8]` (длина среза) до умножения на размер элемента; знаковый —
  `МЕНЬШЕ` на `< 0`, верхняя — `ВЫШРАВ`; сбой → `стд_сбой_границы`, 101.
- `граница_срез` — статика создания: отрицательные константные границы,
  порядок `нач > кон`, для массива `кон > N` (`..`) / `кон >= N` (`..=`).
- `срез_дл` переписан: начало и конец вычисляются по одному разу (раньше
  начало вычислялось дважды); в `надёжно` проверки знака и порядка идут до
  загрузки источника, верхняя граница — до арифметики длины; сбой → 102.
  Для `..=` проверка `кон >= длина` покрывает переполнение `кон + 1`.
- Дефект найден проверкой: два входа в сбой имели разную глубину стека
  (0 и 1), а `пад_и_вызов` выравнивает по статической `ген.глуб`, где
  учитываются и прологовые сохранения регистров — исправлено двумя метками
  `ср_сбойп`/`ср_сбойв` с зафиксированной глубиной каждого входа.
- `ген_пред` — предупреждение компилятора в stderr; срез сырого указателя
  в `надёжно` предупреждает о непроверяемой области.
- Источники среза: массив (граница статическая `N`), срез и строка (длина
  из `{данные, длина}`), сырой указатель — без проверок + предупреждение.

Тесты `тесты/границы/ср_*.раз` (10 файлов): статика — `ср_конст_минус`,
`ср_конст_конец`, `ср_конст_вкл`, `ср_конст_порядок`; динамика —
`ср_инд_дин`/`ср_инд_минус` (101), `ср_созд_конец`/`ср_созд_порядок`/
`ср_созд_вкл` (102), `ср_в_границах` (42: `[1..3]`, `[0..0]`, `[4..4]`,
`[0..=3]`). Прогон 42/42. В `быстро` ИСКРА не содержит `стд_сбой_границы`.

Сиды `разум.exe`/`разум`/`разум_мак` пересобраны, фикс-точка побайтно,
ИСКРА новым РАЗУМОМ байт-идентична, СЛЕД собирается. CI: шаг «Границы
срезов» в трёх джобах. Обновлены README, docs/04 (v0.9), docs/05,
ROADMAP, ЗАДАЧИ, AGENTS.

Не входили: `стр[индекс]`, стабильные аварийные коды как интерфейс,
изменение представлений данных.

## 2026-09-21 — границы строк `стр` в двух режимах

Ветка `фича/границы-строк` от `develop`.

- `индекс_адрес`, ветка `ТВ_СТР`: строка — та же пара `{данные, длина}`,
  что и срез; в `надёжно` индекс проверяется по `[АКК+8]` до адресации —
  знаковый `МЕНЬШЕ` на `< 0`, верхняя `ВЫШРАВ`; сбой → код 101. Элемент —
  `сим`, индекс — смещение байта UTF-8.
- `срез_инд_стат` обобщён до `пара_инд_стат(…, что)` — «среза»/«строки»;
  динамический блок вынесен в `пара_инд_дин` — общий для `ТВ_СРЕЗ` и
  `ТВ_СТР`.
- `стр_срез` в `биб/стр.раз` больше не обход синтаксиса: проверяет
  `от <= по <= строка.длина` всегда и завершает процесс кодом 102. Для
  `выход` добавлено `взять система`; цикл `стр ↔ система` отсекается
  загрузчиком (путь регистрируется до разбора детей).
- Все ~50 существующих вызовов `стр_срез` в кодовой базе проверены на
  ограниченность границ — все легальны; фикс-точка это подтверждает.

Тесты `тесты/границы/ст_*.раз` (8 файлов): статика — `ст_конст_минус`;
динамика — `ст_инд_дин`/`ст_инд_минус`/`ст_инд_длина`/`ст_пустая` (101),
`ст_срез_биб`/`ст_срез_порядок` (102), `ст_в_границах` (42: индекс,
срез синтаксисом, `стр_срез`, пустой срез `[5..5]`). Прогон 50/50.
В `быстро` ИСКРА не содержит `стд_сбой_границы`, статика остаётся.

Сиды всех трёх ОС пересобраны — изменилась `биб/стр.раз`, поэтому бинарь
ИСКРЫ законно отличается; детерминизм сборки подтверждён. Фикс-точка
побайтно, СЛЕД собирается. Попутно зафиксировано: `искра.exe` на пустом
входе падает — дефект существовал до этого этапа.

CI: шаг «Границы строк» в трёх джобах. Обновлены README, docs/04 (v0.10),
docs/05, ROADMAP, ЗАДАЧИ, AGENTS.

Не входили: стабильные аварийные обработчики и коды как интерфейс
(`фича/сбой-границы`), изменение представления строк.

## Этап `фича/сбой-границы` — аварийные обработчики и коды 101/102/103

Дата: продолжение цикла базовой безопасности. Ветка `фича/сбой-границы`
от `develop` (прогон на входе 50/50).

Реализация (`raz/ген.раз`):

- Три обработчика вместо одного общего: `стд_сбой_границы` (101),
  `стд_сбой_среза` (102), `стд_сбой_памяти` (103). Биты `нужны_стд`:
  4/8/16; сайты `выдели`/`новый` ставят 1|16.
- ABI: `СЧТ`=значение, `ДАН`=предел, `ИСТ`=вид (0 чтение, 1 запись,
  2 адрес, 3 срез, 4 память), `БАЗ`=номер строки, `Р8`=файл (резерв, 0).
- Диагностика без `консоль`: `стд_печ_стр`/`стд_печ_чис` пишут в stderr —
  Windows `WriteFile(GetStdHandle(-12))`, SysV `write(2)`; числа знаковые.
  Фрагменты текста эмитируются в секцию данных (`сб_*: БАЙТ "..."`), буфер
  числа — в кадре обработчика; память в аварийном пути не выделяется.
- `стд_выдели`: Windows `HeapAlloc`=0 и SysV `mmap`<0 уходят в
  `стд_сбой_памяти` — записи по нулевому указателю нет. Строка передаётся
  через `БАЗ` (callee-saved, хелперы её не трогают; сайт обрамляет
  `ВСТЕК БАЗ`/`ИЗСТЕКА БАЗ`).
- Сайты: `граница_дин` (массив), `пара_инд_дин` (срез/строка; `ДАН`=длина
  грузится до проверок — нужна диагностике), `срез_дл` (два входа с разной
  глубиной стека сохранены; для массива предел — статическая длина).
- СЛЕД: обработчики и `стд_печ_*` имеют пролог `ВСТЕК УКК/ПЕР УКК,УКС`,
  невозвратные пути закрываются `НОП` — сканер карты (пролог после
  C3/C9/CC/90) видит все метки. Проверено: `точка стд_сбой_границы`
  останавливает процесс на входе обработчика.
- Импорты по битам: GetProcessHeap при 1|2, HeapAlloc при 1, HeapFree при 2,
  GetStdHandle+WriteFile при 4|8|16.

Тесты: `тесты/границы/пам_выдели.раз` (невозможный размер → 103). Прогон
51/51. Примеры stderr: `разум: сбой границы: индекс -3 вне длины 4, вид 1,
строка 5)`, `разум: сбой среза: диапазон 3..1, строка 6)`,
`разум: сбой памяти: размер 4611686018427387904, строка 3)`.

Проверки: фикс-точка побайтно, ИСКРА и РАЗУМ детерминированы, СЛЕД
собирается и сценарий точки работает, сиды трёх ОС пересобраны (изменился
кодогенератор). `быстро` не испускает обработчики, статика сохраняется.

CI: шаг «Сбои — диагностика 101/103» в трёх джобах (stderr + коды);
на Windows дополнительно сценарий СЛЕДа с точкой на `стд_сбой_границы`.

Не входили: `стр_срез` остаётся на `система.выход(102)` без диагностики
(библиотечный уровень); передача имени файла в `Р8` — резерв ABI;
удаление доказанно лишних проверок — следующий этап.

## Этап `фича/оптимизация-проверок` — доказуемое удаление проверок

Дата: продолжение цикла базовой безопасности. Ветка `фича/оптимизация-проверок`
→ `develop`. Цель: опускать динамическую проверку границ только при
доказанном индексе, без ослабления семантики и без опоры на пипхол.

Реализация (`raz/ген.раз`):

- `ГДоказ` — доказанный диапазон переменной цикла: слот, коллекция-источник
  (пусто для числового диапазона), `мин`/`предел`; список `ген.доказаны`
  живёт в контексте функции и снимается при выходе из тела цикла.
- `ген.индекс_доказан(об, инд, длина, пара)` — чистый анализ без эмиссии:
  `инд & (N-1)` и `инд % K` с беззнаковым (`ТВ_БЕЗ`) индексом; переменная
  `для и в мин..предел` при константных границах, восходящем цикле,
  `мин >= 0`, `предел <= длина`; переменная `для и в кол` при индексации
  той же коллекции по идентичности слота (`У_ИМЯ`), включая загрузку
  элемента `для и, э в кол`.
- `_мутирует`/`тело_мутирует` — обход всего дерева тела: присваивание
  (включая составные и поля пары), затенение имени, взятие адреса —
  доказательство не ставится. Для коллекционного цикла проверяется и
  мутация самой коллекции (`ср = другой`, `ср.длина = x`).
- Подключение: `индекс_адрес` — `ТВ_МАСС` через `стат_ок`, `ТВ_СРЕЗ`/`ТВ_СТР`
  через пропуск `пара_инд_дин`; статические ошибки констант неизменны.
- Сырой указатель, нечужие выражения и индекс вне доказанного диапазона —
  проверка остаётся.

Проверки: прогон 61/61 (10 новых кейсов `тесты/границы/опт_*.раз`); фикс-точка
побайтно; ИСКРА детерминирована и байт-идентична (доказуемых паттернов в её
коде нет); СЛЕД собирается; сиды трёх ОС пересобраны. В доказанных кейсах
`стд_сбой_границы` отсутствует в ИСКРЕ уже на `-O0`; недоказанные дают 101;
`быстро` не испускает обработчиков.

CI: шаг «Оптимизация проверок» в трёх джобах — отсутствие проверки в
доказанном цикле, сохранение при мутации (код 101), паритет -O0/-O1,
чистота `быстро`.

Не входили: доказательства для полей (`об.поле[инд]`), вызовов с чистыми
аргументами и линейных индексов вида `и + смещ` — следующие расширения;
сканер мутаций консервативен: запись элемента `кол[i] = x` не считается
мутацией слота пары, но запись поля `кол.длина = x` — считается.

## 2026-09-21 — диагностическая память «--память проверка»

Ветка `фича/диагностика-памяти`. Новая независимая ось `--память
обычно|проверка` (CLI `raz/разум.раз`, поле `Ген.память`, параметр
`ген_новый`, прагма `;память проверка` в ИСКРЕ).

Реализация в `raz/ген.раз`:

- `стд_выдели_пров`: заголовок 48 байт перед областью — размер (+0),
  магия `0x51C0DE51` (+8), строка выделения (+16), связь (+24),
  состояние (+32), резерв (+40); огранка 16 байт `0x5A` за областью;
  область заливается `0xCD`; узлы связаны списком от `стд_пам_голова`.
- `стд_освободи_пров`: обход списка проверяет членство по адресу области
  (чужой указатель — вид 0, дикая память не читается), состояние
  (повторное — вид 1), магию (вид 3), огранку (вид 2); область
  заливается `0xDD`, состояние=2, магия=`0x7EA1DEAD` — карантин без
  возврата ОС.
- `стд_сбой_целост` — стабильный код 104: `разум: сбой целостности
  памяти: адрес N, вид V, строка L)`; ABI обработчиков без изменений.
- `стд_пам_фин` вызывается из `старт` после `ф_главная` до выхода:
  живые узлы проверяются на магию/огранку, утечки печатаются
  (`разум: утечки памяти: N блоков, M байт`) → 104; чистый выход
  возвращает код программы.
- Биты `нужны_стд`: 32 — пакет учёта (голова/выдели/освободи/фин),
  64 — `стд_сбой_целост`; `стд_выдели_пров` испускается только при
  реальном `выдели` (бит 1); маски аварийного вывода расширены до 124.
- Найденные дефекты в процессе: продвижение по списку `ДОБ ДАН,24` вместо
  `ДАН=узел+24` (находился только головной блок); `стд_выдели_пров`
  испускался без `стд_выдели` — висячая ссылка в ИСКРЕ.

Проверки: прогон 69/69 (8 новых кейсов `тесты/память/пров_*.раз`, в
раннере поле `Кейс.арг` для флагов разума); фикс-точка побайтно; сиды
трёх ОС пересобраны; ELF/Mach-O с проверкой собираются без
Windows-импортов; `быстро`+`проверка` работают независимо; в `обычно`
диагностики в ИСКРЕ нет.

CI: шаг «Память — проверка» в трёх джобах — чистый прогон 42, двойное
освобождение и утечка → 104 с диагностикой stderr, отсутствие `стд_пам`
в `обычно`.

Ограничения (зафиксированы в каноне): запись в освобождённый блок не
перехватывается (область лишь отравлена `0xDD`); прямой `система.выход`
обходит `стд_пам_фин`; проверка членства идёт по списку, а не по
границам сырого указателя.

Документы: README, docs/04 (v0.13), docs/05 (§15 «Диагностическая
память», §16), ROADMAP, ЗАДАЧИ, AGENTS, этот лог.

## фича/типы-размер-смещение (2025-09-21)

- Парсер: `размер`/`смещение` — типы без уровня в `_тип_имя`; внутренне
  `без64`/`цел64`, текст типа канонический — кодогенератор и суффиксный
  разбор разрядности не тронуты, ABI прежний.
- Совместимость: `размер`↔`нат.верх`, `смещение`↔`цел.верх` — тот же
  тип; `размер`↔`смещение` без `как` — ошибка несовместимости (знак не
  переносится). Диагностика печатает каноническое имя.
- Семантическая миграция `биб/`: `память` (колво/инд → размер),
  `список` (длина/вмест/слоты указателей → размер), `буфер`
  (длина/вмест/поз → размер), `стр` (длина/поз/от/по → размер,
  `стр_найди` → смещение), `узлы` (строка/столб → размер), `сист.файл`
  и `система`/`консоль` на SysV (разм/записано/прочитано → смещение,
  счётчики → размер), `процесс` (аргв/са/си64/пи64 → размер).
  Значения, хендлы, fd, коды и битовые поля оставлены `нат.верх`/`цел.верх`.
- Подсветка VS Code: `размер`/`смещение` добавлены в storage.type.
- Тесты: `тесты/типы/` — объявления/арифметика/параметры/возврат/
  указатели/совместимость (42), индексация массивов/срезов/строк (142),
  два кейса-ошибки несовместимости.

Проверки: прогон 73/73; фикс-точка самосборки побайтно; сиды всех трёх
ОС и СЛЕД пересобраны; ИСКРА детерминистична.

CI: шаг «Типы — размер/смещение» в трёх джобах — компиляция и коды
двух позитивных кейсов + отказ и диагностика на несовместимом
присваивании.

Следующий этап: `фича/размер-смещение-ядро` — миграция raz/иск/след
по тому же семантическому правилу (в `иск/` `нат.верх` — фиксированные
поля форматов и остаётся).

Документы: README, docs/04 (v0.14), docs/05 (§6 «Машинные слова»),
ROADMAP, ЗАДАЧИ, AGENTS, editors/vscode, этот лог.

## 2026-09-21 — миграция ядра на `размер`/`смещение`

Ветка фича/размер-смещение-ядро. Семантическая миграция
`нат.верх`/`цел.верх` → `размер`/`смещение` в `raz/`, `иск/`, `след/`
по правилу канона §6; механических замен без анализа не делалось.

- `raz/`: строки/столбцы и позиции лексера и парсера → `размер`;
  смещения полей/локалей генератора → `смещение`; счётчики и размеры
  (`Ген.глуб`, `фрейм`, `рег_локов`, `стр_сч`, `мет_сч`, пределы
  доказательств, `пад`/`тень`/`назад` пролога) → `размер`; индексы
  «или -1» (`инд_арг`, `найден`, `инд_р`, `цифра16`) → `смещение`.
  Ошибки миграции выявила строгая типизация: `сумма`/`пад` собираются
  знаковой арифметикой и приводятся `как размер` явно.
- `иск/`: `Опер.разм`/`Опер.масш` → `размер`; размеры секций, смещения
  размещения, VA/RVA планов, `_схр`/`_встр` указатели-как-слова →
  `размер`; дисплейсменты и «регистр или -1» (`база`/`инд` в `_рм`,
  `Слаг.знак`, `глуб`) → `смещение`; опкоды, байты кодировки
  (`_рекс`, `_сиб`, `байт_мр`), поля форматов (`флаги`, `кмд`,
  значения `уст16/32/64`) и значения выражений оставлены
  `нат.верх`/`цел.верх`.
- `след/`: адреса точек останова, смещения PE-разбора, счётчики →
  `размер`; `точка_найди`, `рег_инд`, `разд` в разборе адреса →
  `смещение`; хендлы, идентификаторы потоков, коды событий и внешние
  сигнатуры `ипп.раз` оставлены.

Проверки: прогон 73/73; фикс-точка самосборки побайтно; `искра.exe`
байт-идентична прежней (переименования типов не меняют кодогенерацию —
`размер`/`смещение` синонимы `без64`/`цел64`); сиды ELF64/Mach-O и
СЛЕД пересобраны; сигнатуры 7F 45 4C 46 / CF FA ED FE подтверждены.

Документы: README, docs/04 (v0.15), ROADMAP, ЗАДАЧИ, AGENTS, этот лог.

## 21.09 — этап `фича/видимость-модулей`

Видимость верхнеуровневых объявлений `открыто`/`скрыто`:

- `биб/узлы.раз`: у `Узел` поля `мод` (модуль-владелец) и `отк`.
- `raz/парсер.раз`: префиксы `открыто`/`скрыто` перед `запись`/`переч`/
  `тип`/`конст`/`функ` (включая методы); без префикса — скрыто.
- `raz/загрузчик.раз`: путь модуля проставляется каждому объявлению.
- `raz/ген.раз`: поля `мод`/`отк` в таблицах символов, `мод_тек`/`стр_тек`,
  хелпер `пров_видим`; проверка при разрешении функций, констант,
  синонимов, записей, перечислений и методов. Ошибка:
  «имя «X» скрыто в модуле «файл»».
- Библиотека и внутренние модули размечены `открыто` по факту
  использования; в `след/след.раз` помощник `шест` открыт для `пе.раз`.

Тесты `тесты/модули/`: `вид_откр` (открытые функция/константа/запись +
скрытый помощник внутри модуля) → 42; `ошибка_скрытая_*` — отказ с
диагностикой для записи, константы и функции.

Проверки: прогон 77/77; фикс-точка самосборки побайтно; шесть сидов и
СЛЕД пересобраны (MZ/7F454C46/CFFAEDFE); CI-шаг «Видимость модулей» в
трёх джобах. Инцидент: искра.exe была обнулена перенаправлением
`> искра.exe` при сборке её же — восстановлена из git и пересобрана
через временное имя.

Документы: README, docs/04 (v0.16), docs/05 (§8 «Видимость»), ROADMAP,
ЗАДАЧИ, AGENTS, подсветка VS Code, этот лог.

## 21.09 — этап `фича/рми-кэш`

Интерфейсный кэш модулей `.рми` — загрузка без перечтения исходников:

- `raz/кэш.раз` (новый): формат `.рми` (заголовок `;РМИ 1`, цель, режим,
  память, опт), интерфейс и фрагменты кода/данных, хеши исходника и
  зависимостей, проверка ключа актуальности.
- `биб/узлы.раз`: вид `У_РМИ` — маркер кэшированного модуля.
- `raz/загрузчик.раз`: состояние кэша в `Загр`, чтение `.рми` с
  инвалидацией; при неуспехе — обычный разбор исходника.
- `raz/парсер.раз`: `разбери_тип` — тип из строки интерфейса.
- `raz/ген.раз`: `тип_стр`, сериализация и реплей интерфейса (записи,
  константы, синонимы, перечисления, функции, методы, внешние);
  per-module буферы `ГМодБуф`, биты `нужны_стд`; метки привязаны к
  имени функции со сбросом счётчика — выход из кэша побайтно равен
  выходу из исходников.
- `raz/разум.раз`: флаг `--кэш вкл|выкл` (умолчание вкл); корневой
  модуль не кэшируется.
- Попутное исправление: элементы перечислений (`Переч.элем` и голое
  имя) не работали в выражениях — `тип_выр` падал раньше проверки
  перечислений; добавлен `переч_элем_ищи` с проверкой видимости.

Тесты `тесты/модули/`: `кэш_мод.раз` + `кэш_польз.раз` (функция,
константа, запись+метод, перечисление обеими формами, синоним) → 41;
`ошибка_кэш_скрытая.раз` — скрытая функция из кэша недоступна;
`тесты/типы/перечисление.раз` → 43. Инвалидация по исходнику, цели и
режиму проверена вручную.

Проверки: прогон 80/80 (двойной — чтение кэша тоже); фикс-точка
побайтно; шесть сидов и СЛЕД пересобраны; CI-шаг «Кэш интерфейсов
.рми» в трёх джобах; `*.рми` в .gitignore.

Документы: README, docs/04 (раздел модулей + v0.17), docs/05 (кэш
.рми), ROADMAP, ЗАДАЧИ, AGENTS, этот лог.

## 21.09 — этап `фича/имена-типов`

Исходные имена типов в диагностике:

- `Узел` типа: `знач2` теперь хранит разрядность числового типа (для
  `ТВ_МАСС` — по-прежнему длина), `текст` — написанное имя.
- `raz/парсер.раз`: `размер` → `ТВ_БЕЗ` + `текст="размер"` +
  `знач2=64`; `смещение` → `ТВ_ЦЕЛ` + `"смещение"` + 64; составные
  формы `цел.верх`/`нат.шаг` сохраняют своё имя и разрядность.
- `raz/ген.раз`: конструкторы `т_цел64`/`т_без64`/`т_без8` заполняют
  `знач2`; `ген.размер` читает `знач2` вместо суффикса `текст`;
  `имя_типа` печатает написанное имя для числовых типов и рекурсивно
  форматирует указатели/срезы/массивы.
- `биб/узлы.раз`: комментарий к `знач2` уточнён.

Диагностика: `требуется размер, получено смещение` вместо
`требуется нат.верх, получено цел.верх`. Совместимость и ABI не
изменились; `.рми` хранит написанные имена и читает старые `без64`.

Проверки: прогон 80/80 (кейсы ошибок уточнены до полного текста),
фикс-точка побайтно, сиды разума пересобраны, СЛЕД собран, ИСКРА
байт-идентична (кодогенерация не затронута).

Документы: docs/04 (v0.18), docs/05 (диагностика в §машинные слова),
ЗАДАЧИ, AGENTS, этот лог.

## 21.09 — этап `фича/версии-конвейер`

Прагма `;версия N` и конвейер устаревания (канон §11):

- `raz/парсер.раз`: `ВЕРСИЯ_КАНОНА = 1`; `_версия_исх` разбирает прагму
  первой строки; `разбери_ф(исх, путь)` проверяет номер — будущий, ноль
  и без номера — ошибки; поддерживаются последние 3 версии. Поля
  `Парсер`: `версия`, `путь`, `тихо`, флаги `пред_бул`/`пред_плос`.
- Предупреждения `_пред` в stderr, раз на конструкцию в файле, с путём
  и строкой: `бул` → `лог`; плоские `целN`/`безN` → `цел.*`/`нат.*`
  (`_уровень_имя` — обратное отображение бит в уровень). `разбери_тип`
  тихий — `.рми` не предупреждает. `:=` предупреждал и раньше.
- `raz/загрузчик.раз`: `разбери_ф(исх, путь)` — путь в предупреждения.
- `биб/процесс.*` (3 платформы): `_запусти` с флагом `слей`; открытые
  `запусти` и `запусти_слей` — stderr ребёнка в файл вывода.
- Тестовые файлы мигрированы: `*без8` → `*нат.старт`, `= без64` →
  `= нат.верх` (10 файлов в тесты/память, границы, модули).
- `д32`/`д64` — уже вычеркнуты из ИСКРЫ (канон: `БАЙТ`/`СЛОВО`/
  `ДВСЛОВО`/`КВАД`), отдельной работы не требуют.

Тесты `тесты/версии/`: `версия_1` (код 7), `версия_новая`/`_ноль`/
`_без_номера` — ошибки; `уст_плоский`/`уст_бул`/`уст_присв` —
предупреждения, ловятся `запусти_слей` кейсами `ок предупр`.

Проверки: прогон 87/87, фикс-точка побайтно, сиды разума и ИСКРЫ
пересобраны (MZ/ELF/Mach-O сигнатуры верны), СЛЕД собран, разум
собирается без единого предупреждения.

Документы: README, docs/04 (v0.19), docs/05 (§11 уточнён), ROADMAP
(`;версия` ИСКРЫ и `переведи` — следующие этапы), ЗАДАЧИ, AGENTS,
этот лог.

## 21.09 — этап `фича/версия-искра`

`;версия` в лексере ИСКРЫ и эмиссия в `.иск`:

- `иск/искра.раз`: `ИСКРА_ВЕРСИЯ = 1`; `_прагмы` разбирает ведущий блок
  `;`-строк — прагма `;слово` без пробела после `;`, «; комментарий»
  с пробелом пропускается; несколько прагм на строке и неизвестные
  имена допускаются (совместимость вперёд). `;цель` не обязана быть
  первым словом первой строки. `_строка_n` вместо `_первая_строка` и
  `_цель_из`. Ошибки: будущий номер, ноль, `;версия` без номера —
  `искра: строка N`.
- `raz/ген.раз`: эмиссия `;версия <ВЕРСИЯ_КАНОНА>` в каждый `.иск`.
- Тесты `тесты/версии/иск_версия_*.иск` — три ошибки прагмы ИСКРЫ;
  прогон `проверь_искру` + кейсы `ок искра`.
- Замечено: пустой `.иск` (только `СЕКЦИЯ код`) падает segfault и на
  старом сиде — пред-существующий дефект, отдельный этап.

Проверки: прогон 90/90 (каждый `.иск` теперь несёт `;версия 1`),
фикс-точка побайтно (два поколения), все шесть сидов и СЛЕД
пересобраны; `;версия 1 ;цель linux` на одной строке → ELF.

Документы: docs/02 (раздел «Прагмы шапки»), docs/04 (v0.20),
docs/05 (§11), README, ROADMAP, ЗАДАЧИ, AGENTS, этот лог.

## 2025 — `фича/переведи`: мигратор исходников

- `переведи/переведи.раз` — программа на РАЗУМЕ: CLI
  `переведи файл.раз [файл2.раз]`, переписывание на месте; код
  выхода — число необработанных файлов (0 — всё ок, 2 — нет аргументов).
- Правила машиночитаемы (`запись Правило` + `правила()`): `бул`→`лог`;
  `цел8…128`→`цел.старт…пик`; `без8…128`→`нат.старт…пик`;
  `имя := выр`→`введи имя = выр`.
- Заменяются только идентификаторы кода: строковые литералы,
  комментарии (`#`, `;`, прагмы `;версия`) и тела `искра:`-блоков
  копируются побайтно; `\n`/`\r\n` и конец файла сохраняются.
- Идемпотентность: повторный запуск — «без изменений»; несуществующий
  файл — stderr + ненулевой код.
- `биб/процесс.*`: `путь_переведи()` на трёх платформах; прогон —
  `проверь_переведи` (побайтная сверка с эталоном, идемпотентность,
  компиляция мигрированного → код 41) и `проверь_переведи_ош`
  (несуществующий файл). Шаг сборки переведи в трёх джобах CI.
- Найдено: `стр_срез` завершает процесс кодом 102 при выходе за границу —
  в переведи это ловилось на последней строке без `\n`; срез
  разделителя вычисляется с учётом конца файла.

Проверки: прогон 92/92; миграция фикстуры побайтно совпала с эталоном;
мигрированный файл компилируется и даёт код 41; CRLF сохраняется.

Документы: docs/04 (v0.21), docs/05 (§11 «Мигратор переведи»), README,
ROADMAP, ЗАДАЧИ, AGENTS, этот лог.

## 2025 — `фича/канон-версии`: отметки биб и тесты/канон

- Отметки стабильности (канон §11): первая строка каждого модуля
  `биб/` — `;стабильно` (17: память, стр, список, буфер, основа,
  консоль.*, система.*, сист.файл.*, мат, мат.цел, мат.нат) или
  `;развивается` (7: узлы, процесс.*, файл.*). Для лексера отметка —
  обычный `;`-комментарий, разбор не затронут.
- `тесты/канон/версия_1/программа.раз` — представительная программа
  под `;версия 1`: записи с литералом `Точка{икс:…}`, перечисление
  (обе формы элемента), константа, массив, срез, метод, `для`,
  строгий лог — код 42, кейс `канон версия 1` в прогоне.
- CI: шаг «Отметки стабильности биб» в трёх джобах — каждый
  `биб/**/*.раз` обязан иметь `;стабильно`/`;развивается` в шапке.

Проверки: прогон 93/93; программа версия_1 компилируется и даёт 42;
все 24 модуля биб собираются с отметками (прогон + самосборка).

Документы: docs/05 (§11 стабильность + тесты/канон), ROADMAP, ЗАДАЧИ,
AGENTS, этот лог.

## 2026-09-21 — починка CI и слияние develop → main

`develop` впервые запушен на GitHub: выявлена серия дефектов,
невидимых локально.

- `ci.yml`: двоеточие в имени шага «Память — проверка: …» ломало
  YAML — workflow не стартовал (коммит 043f1dc, был ранее).
- `биб/система.linux.раз`, `биб/система.macos.раз`: `аргументы()`
  возвращал argv с NUL-разделителями (`/proc/self/cmdline`, байт 0
  после каждого аргумента). `_слово_с` пропускает только пробелы/табы
  → `--режим быстро` разбирался неверно, разум завершался кодом 2.
  Теперь разделители — пробелы, как у GetCommandLineW на Windows
  (правка/аргументы-argv, merge 316b7f6).
- `ci.yml`: кириллическая переменная `код` в bash-шагах — bash
  трактует `код=101` как команду → exit 127. Заменена на `rc`
  (коммит 90661a5).
- `raz/ген.раз`: `стд_выдели` проверял ошибку mmap по-линуксовому
  (АКК < 0). На macOS ошибка syscall — флаг переноса + положительный
  errno: ENOMEM=12 трактовался как указатель → segfault вместо 103.
  Для цели macos испускается ПЕРЕНОС (коммит с сидами).
- `ci.yml`: pwsh-раннер дописывает `exit $LASTEXITCODE` — шаги,
  кончающиеся ожидаемо ненулевой командой, падали. Добавлен `exit 0`
  в шесть шагов (коммит 616ca50).
- `ci.yml`: `след.exe` — артефакт, не в репозитории; добавлен шаг
  «СЛЕД — сборка» (коммит cb66c83).

Итог: CI на `develop` (cb66c83) — зелёный на Windows/Linux/macOS.
`develop` слит в `main` (merge 74b8d5e); CI на `main` — зелёный.
Локальный прогон 93/93, фикс-точка побайтно.
<<<<<<< HEAD

## 2026-09-21 — веха `фича/вещ-верх`: xmm/SSE2 + тип `вещ.верх` (f64)

Первая часть вехи `вещ.*`/`век`: плавающая точка f64 от ИСКРЫ до РАЗУМА.

- `иск/рег.раз`: xmm-регистры `ПЛАВ0..ПЛАВ15` (`xmm0..15`), `разм=16`
  как признак класса; запрет xmm в адресных операндах.
- `иск/кодер.раз`: префиксные SSE2-формы — `ПЕРВД` (movsd),
  `ДОБВД/ВЫЧВД/УМНВД/ДЕЛВД/КОРНВД`, `ПРОВВД` (ucomisd),
  `ЦЕЛВД`/`ВЕЩЦД` (cvtsi2sd/cvttsd2si).
- `иск/парсер.раз`: разбор xmm-операндов и `КВАД[...]` для f64-констант.
- Побочно найден и исправлен пред-существующий дефект `ВЫРОВНЯТЬ`
  в секции данных: `(0 - нач) % гран` давал отрицательный остаток
  в `размер` → зависание.
- `raz/лексер.раз`: токен `ТК_ВЕЩ` — вещественные литералы
  (точка, экспонента, подчёркивания).
- `raz/парсер.раз`: тип `вещ.верх`, машинное имя `вещ64` в `.рми`,
  атом `У_ВЕЩ`.
- `raz/ген.раз`: выражение f64 возвращает `ПЛАВ0`; литералы —
  компиляторная конверсия decimal→binary64 (`вещ_биты`: 64-битная
  мантисса + масштаб б2, УМНБ/ДЕЛБ, округление к ближайшему чётному,
  денормалы; переполнение — ошибка); NaN-корректные сравнения;
  явные `как`-приведения цел↔вещ; ABI-маршаллинг смешанных
  аргументов (Windows — xmm по позиции, SysV — отдельный счётчик);
  `отложи` сохраняет `ПЛАВ0`; составное присваивание — диагностика.
- Тесты: `тесты/вещ/верх.раз` (255) + четыре кейса ошибок;
  прогон 98/98, фикс-точка поколений 2↔3 побайтно.
- Пересобраны все шесть сидов (win/linux/macos) новой цепочкой.

Отдельный урок: `разум ... > файл 2>&1` прячет диагностику в выход —
цепочка `&&` собирала устаревший бинарь; повреждённая правка
(`о  ператор`) обнаружилась только на `--цель linux`.

## вещ.рост (f32) — ядро типа

- ИСКРА: скалярные f32-мнемоники `ПЕРВС` (movss), `ДОБВС/ВЫЧВС/УМНВС/ДЕЛВС`
  (addss/subss/mulss/divss), `СРАВВС` (ucomiss), `ЦЕЛВС` (cvtsi2ss),
  `ВЕЩЦС` (cvttss2si), `ВЕЩРВ`/`ВЕЩВР` (cvtss2sd/cvtsd2ss), `ИИЛИПС`
  (xorps); операнд `ДВСЛОВО[...]` для m32, константы — `ДВСЛОВО`.
- РАЗУМ: `вещ.рост` в парсере; уровень — `знач2` узла `ТВ_ВЕЩ` (32/64);
  `вещ_биты` параметризована форматом (binary32/64, бит-точно);
  литерал принимает уровень контекста через `_пометь_лит` (объявление,
  параметр вызова, второй операнд бинарной операции, элемент литерала
  массива); смешение уровней без `как` — ошибка «несовместимые
  вещественные уровни».
- Аргументы вызовов теперь проверяются для вещественных параметров:
  раньше литерал эмитился как f64 независимо от параметра.
- Тесты: `тесты/вещ/рост.раз` (255) + три кейса ошибок;
  прогон 102/102, фикс-точка поколений 2↔3 побайтно; все шесть
  сидов пересобраны.
- Замечено: 0.1f+0.2f == 0.3f в binary32 (обратный к f64 квирк),
  но 0.3f+0.6f != 0.9f — зафиксировано в тесте.

## век — 128-битные SIMD-векторы (фича/век)

- ИСКРА: квалификатор `ОКВАД` (m128); пакетные мнемоники —
  `ПЕРПС`/`ПЕРПД`/`ПЕРЦВ` (movups/movupd/movdqu), `ДОБПС`/`ВЫЧПС`/
  `УМНПС`/`ДЕЛПС`/`МИНПС`/`МАКСПС`/`КОРНПС` и ПД-варианты, целые
  `ПДОББ/С/Д/К`, `ПВЫЧ*`, `ПУМНС`, `ПИ`/`ПИЛИ`/`ПИИЛИ`/`ПИНЕ`,
  `ПСРАВБ/С/Д`, `ПБОЛЬБ/С/Д`, `СРАВПС`/`СРАВПД` (cmpps imm8),
  `ПСДВИГД` (pshufd). Проверено исполнением addps на железе.
- РАЗУМ: `век Т х N` в парсере и `.рми` (компактно `векN:элем`);
  ширина строго 16 байт (`|эл| × N = 128 бит`), иное — ошибка.
  Поэлементные операции, литералы, индексация, составные
  присваивания, xmm-ABI (стековый вектор — два 8-байтных слота),
  `отложи`. Целочисленное `×` — только лейны-слова; `÷` для целых
  отклонено.
- Найдено и исправлено по ходу: сравнение суффикса лейна по
  указателю вместо `стр_равны`; дисбаланс стека при вызовах —
  `назад` считал 8 байт на аргумент вместо фактического `набег`;
  `тип_выр` возвращал `лог` для `==` над векторами (маска);
  `_век_компакт` резал «век» по 3 байтам вместо 6 (UTF-8) —
  `.рми`-десериализация давала ТВ_ИМЯ «век4»; парсер терял номер
  строки на `+ − × ÷ << >>` («строка 0» в ошибках).
- Тесты: `тесты/век/век.раз` (255, 8 групп) + 5 кейсов ошибок;
  `.рми`-кэш с век-типами проверен в обе стороны.
- Прогон 108/108, фикс-точка 2↔3 разума и искры побайтно,
  все шесть сидов пересобраны.

- `повтор(скаляр)` — встроенное распыление в вектор: новый узел
  `У_ПОВТОР`, разбор в атоме, `тип_выр` строит `век Т х (16/|Т|)` по
  типу аргумента; кодогенерация — f32/f64 одним `pshufd` (imm 0/68),
  целые через 16-байтовый слот по шагу лейна + `movdqu`. Нечисловой
  аргумент — ошибка «повтор: нужен числовой скаляр».
- Проверка на нейросетях (`_внн.раз`, `_внн4.раз`, кольцо 2-16-16-1):
  скалярная f64 66.7с → векторная с `век{...}`-распылением 39.9с →
  с `повтор` (pshufd) 18.3с = 3.64x; батч-вариант (лейн=пример,
  скалярные веса, `лейнсум` градиентов) 20.5с — обе 400/400.
- Прогон 109/109, фикс-точка 2↔3 побайтно, сиды разума ×3
  пересобраны (искра без изменений — ПСДВИГД уже был).

- Зондирование стека Windows: рамка функции больше одной страницы
  (4096 байт) перескакивала сторожевую страницу — первое касание
  глубокой локали (обнуление большого массива) давало отказ доступа.
  Пролог при цели windows и фрейме > 4КБ теперь эмитит цепочку
  «ПРОВ БАЙТ[УКК-N], 0» сверху вниз по страницам. Найдено на
  нейросети распознавания цифр (фрейм ~57КБ, вещ.рост[14400]).
- Регрессия: тесты/стек_зонд.раз (14400 f32 локально -> 42);
  прогон 110/110, фикс-точка 2<->3 побайтно, сиды разума x3.
- Проверка «сети посложнее»: распознавание 6x6-цифр 0..9, датасет
  400 зашумлённых глифов (~10% инверсий), сеть 36-32-16-12 на
  век вещ.рост х 4. Итог: 400/400 на обучающих, 1000/1000 на
  свежих зашумлённых, 10/10 чистых глифов с ~99% уверенности,
  6000 эпох за 17.2с.

- Сеть ~5М параметров (_ш5м.раз): классификация процедурных фигур
  64x64 на 8 классов (круг, кольцо, квадрат, рамка, крест, Х,
  полосы) — 4096->1152->288->8 = 5 054 120 параметров, веса в куче.
  6 эпох: 197->466/480, свежие 733/800 (91.6%), чистые 7/8, ~66.8с.
- Интринсик сумма(век): горизонтальная сумма лейнов в скаляр
  (f32: pshufd 177+addps, pshufd 2+addss; f64: pshufd 78+addsd;
  целые — слот 24 байта с накоплением). Замена лейнсум в _ш5м:
  66.8с -> 66.1с — редукции оказались ~1% рантайма (матричные
  проходы доминируют), точность идентична. Сама редукция ~4x
  быстрее петли извлечений.
- Контекстность встроенных форм: повтор/сумма разбираются как
  интринсики только при следующей '(' — переменные с такими
  именами остаются валидными (найдено на ген.раз:2601 «сумма % 16»
  и 5 тестах прогона). Побочно закрыты краши: верни повтор(век)/
  сумма(не-век) шли в кодоген минуя тип_выр — добавлены проверки
  в выр-ветках обоих узлов.
- Латентная проблема .рми: кэш валидируется хешем исходника, но
  не версией фронтенда — .рми, записанный старым парсером,
  принимается новым без перепарсинга (замаскировало конфликт
  «сумма % 16» на windows-цели). Отдельный этап — метка версии
  фронтенда в .рми.
- Регрессия: тесты/век/ош_сумма.раз; век-тест дополнен проверками
  сумма для f32/f64/слова/байты/двойные слова. Прогон 111/111,
  фикс-точка 2<->3 побайтно (--кэш выкл), сиды разума x3.
- Фазовый профиль _ш5м.раз (1 эпоха, QPC, два устойчивых запуска):
  прямой 4096->1152 — 6594–6695мс, обновление этого слоя —
  2733–2775мс; все остальные фазы вместе — ~511мс. Причина —
  прямой проход читал w1т с шагом 4608 байт при внешнем блоке.
- Перестановка циклов в прямых проходах на вход->блок дала
  последовательный доступ к весам и одно повтор(вход) на строку:
  первая фаза 6594->2338мс, вторая 256->189мс. Пропуск нулевых
  строк бинарного изображения: 2338->951мс (forward) и
  2605->1039мс (update). Отдельный путь для входа 1.0 убрал
  лишнее packed-умножение. Полный запуск 6 эпох: 66.8->13.3с
  (5.02x), качество без изменений: 733/800, чистые 7/8.
- --режим быстро не влияет на этот тест (14.65->14.56с до
  специализации единицы): горячие данные идут через сырые указатели,
  а доказанные локальные индексы уже не получают проверок границ.
- Список активных пикселей отклонён: индексы нат.шаг дали 14.81с,
  готовые смещения строк нат.рост — 14.59с против 13.31с при
  последовательном сканировании. Косвенная загрузка дороже 4096
  предсказуемых проверок на пример.
- Первый слой выделен в локальные ядра слой1_прямой/слой1_обнови.
  Входной массив передаётся вместе с индексом базы: арифметика
  сырых указателей байтовая, поэтому «вхм + бс» для *вещ.рост
  сдвигало адрес на бс байт, а не элементов. Исправленный контракт
  сохраняет 733/800; граница функций стоит ~2.3% (13.94 против
  повторно измеренных 13.64с) и служит точкой для плиточного ядра.
- Gradient batch-4 отменён revert-коммитом 4690d0e: лучший
  последовательный SGD подтверждён повторно — 13.60с, 465/480 и
  733/800.
- Новая задача для модели ~30М: amodal semantic segmentation 64x64.
  В сцене 1–4 перекрывающиеся фигуры (круг, квадрат, крест, кольцо),
  вход хранит видимую яркость с шумом, выход — четыре независимые
  полные маски, включая скрытые части. Детерминированная проверка
  64 сцен: 56 604 положительных пикселя, 5 578 перекрытий.
- Бюджет U-Net 1-32-64-128-256-512-1024 с двумя 3x3-свёртками на
  уровень, skip-связями и 2x2 up-conv: 31 051 296 весов без смещений.
- Первое CNN-ядро: Conv 3x3 1->32, padding=1, выход по 8
  век вещ.рост х 4 на пиксель, затем ReLU. На последней сцене
  контрольная сумма 1930; генератор 64 сцен + свёртка — 0.077с.
- Обобщённая packed-свёртка Cin->Cout хранит каналы блоками по 4;
  каждый входной лейн умножается на вектор четырёх выходных весов.
  Проверка Conv 3x3 32->32: 9 216 параметров, контроль 552;
  генератор 64 сцен + два conv-слоя — 0.116с.
- Добавлены packed max-pool 2x2 и Conv 3x3 32->64 на сетке 32x32
  (73 728 параметров). Контроль уровня 85; генератор и encoder до
  первого 64-канального слоя — 0.150с.
- Encoder расширен до 16x16x128: Conv 64->64, pool, Conv 64->128
  и Conv 128->128 (258 048 новых параметров). Контроль 16;
  генератор и весь текущий encoder — 0.258с.
- Encoder и bottleneck завершены уровнями 8x8x256, 4x4x512 и
  2x2x1024. Encoder-часть содержит ~18.87М весов; контроль
  bottleneck 535, генератор и полный encoder — 0.641с.
- Первый decoder-уровень: up-conv 1024->512, skip-конкатенация
  512+512, Conv 1024->512 и Conv 512->512. Модель достигла ~28.05М
  весов; контроль decoder 13 188, текущий полный forward — 1.370с.
- Добавлена визуализация: ASCII-вход 32x32, совмещённая карта целей
  C/S/X/O с «@» на перекрытиях, нормализованные feature maps encoder
  16x16, bottleneck 2x2 и decoder 4x4. В PGM записываются вход,
  четыре целевые маски и четыре уровня признаков (`_сегм_*.pgm`).
  Forward с терминальной визуализацией и записью файлов — 1.605с.
- Отдельная программа `_pgm_консоль.раз` читает P2-PGM, проверяет
  заголовок, масштабирует блочным средним и рисует палитрой
  ` .:-=+*#%@`. Большие 64x64 выводятся как 64x32 с поправкой на
  пропорции терминала, малые feature maps — без потери строк.
  Проверка показала все 9 файлов. Запуск из корня:
  `raz/разум.exe < _pgm_консоль.раз > _pgm_консоль.иск`, затем
  `искра.exe < _pgm_консоль.иск > _pgm_консоль.exe` и
  `_pgm_консоль.exe`.
- Для совместимости с кодовой страницей PowerShell PGM переименованы
  в ASCII (`_seg_input.pgm`, `_seg_target_circle.pgm` и т.д.),
  диагностические строки просмотрщика также ASCII; содержимое карт
  теперь выводится без mojibake в заголовках.
- Завершён decoder U-Net до `64x64x4`: up-conv и skip-связи уровней
  512→256→128→64→32, финальный 1x1 Conv 32→4 с векторной сигмоидой
  (`эксп4` — ряд Тейлора на x/64 и шесть возведений в квадрат).
  Добавлены `покажи_предсказания` (ASCII-карта C/S/X/O, `@` — несколько
  классов) и `запиши_маску_pgm`; записываются `_seg_decoder64.pgm` и
  четыре `_seg_pred_*.pgm`. Полный forward с визуализацией — 2.276с;
  контрольная сумма выхода 8192 (необученная сеть: ~0.5 на лейн).
  Просмотрщик показывает 14 PGM. Параметры: 31 087 008 весов +
  7 012 смещений.
- Добавлены метрики и визуальная оценка качества: скалярный `лог_нат`
  (редукция по степеням двойки + ряд atanh), `печать_вещ` с тремя
  знаками, BCE-потеря по 16 384 выходам, IoU каждого класса и средний
  IoU, ASCII-наложение `#`-верно/`+`-ложное/`-`-пропуск/`@`-частично
  и карта `_seg_diff.pgm` (0/85/128/170/255). На необученной сети:
  потеря 0.693 (= ln2, эталон), IoU0=0.167 IoU1=0.456 IoU2=IoU3=0,
  средний 0.156. Просмотрщик показывает 15 PGM.
- Реализован полный обратный проход U-Net: `релу_гр4`, `вклад4`
  (градиент входа через сумму по выходным лейнам), `гр_свертка`,
  `гр_свертка_1_32`, `гр_макс_пул2` (argmax-маршрутизация дельты),
  `гр_соедини` (расщепление skip-градиента), `гр_вверх2`,
  `гр_выход_масок` (дельта sigmoid+BCE: p−t). Градиенты накапливаются
  в зеркальных `г_*`-буферах (~124 МиБ), дельты слоёв — в `д_*`.
  Инициализация весов заменена на знакопеременную `случ_веса`
  (одинаковый 0.001 давал симметрию каналов — обучение невозможно).
  Обучение SGD по сценам: потеря и IoU печатаются на каждой сцене и
  усредняются по эпохе; финал — контрольная сцена с фиксированным
  зерном 424242 и полная визуализация.
- Стабилизация обучения: дельта выхода нормируется на 4096 пикселей
  (0.000244140625 = 1/2^12 — точное значение f32), положительные
  пиксели взвешены x8 против дисбаланса фона (~5.4% маски), градиент
  клиппуется поэлементно в ±1.0 (`зажми` в `обнови4`). Без нормировки
  SGD взрывался (потеря 7.7→NaN) или схлопывался в «всё фон».
  Средний IoU считается только по классам, присутствующим в сцене.
  Прогон 6 эпох x 24 сцены (144 обновления, ~23 мин), шаг 0.08:
  потеря по эпохам 0.501, 0.233, 0.205, 0.245, 0.189, 0.240;
  IoU 0.108, 0.221, 0.276, 0.257, 0.290, 0.278. На контрольной
  сцене (зерно 424242): круг сегментирован с IoU=0.897, объект
  находится точно, но каналы других классов ещё частично огняются
  на том же регионе — нужно больше обновлений для разделения
  классов. Стоимость ~9.4 с на сцену (forward+backward+метрики).

## 2026-09-22 — оптимизация кодогенератора, этап А (ROAD_MAP_2)

- Создан ROAD_MAP_2.md: план качества кода по измеренному профилю
  (внутренний цикл conv ~57 инструкций вместо ~10). Ссылка в AGENTS.md.
- Этап А, пункты А2+А4 (ген.раз, бин): константные операнды без стека.
  x+K/-K/&K/|K/^K -> ДОБ/ВЫЧ/И/ИЛИ/ИИЛИ АКК,imm; x*2^k -> СДЛ АКК,k
  (x*1 — пусто, x*0 -> ПЕР АКК,0, прочее -> УМН АКК,АКК,imm);
  x<<K/>>K -> СДЛ/СДП(А) АКК,k&63; сравнения x ? K и K ==/!= x ->
  СРАВ АКК,imm. Ограничение: константа должна помещаться в знаковый
  imm32 (_в_имм32) — imm32 знаково расширяется, иначе неверно
  (нашлось на УМН АКК,АКК,6364136223846793005 — ошибка искры).
  Побочный эффект левого операнда сохраняется (выр.а вычисляется).
- Проверки: фикс-точка самосборки (сам2==сам3), нативный прогон
  111/111, сиды разум.exe/разум/разум_мак пересобраны кросс-эмиссией
  (--цель + искра.exe читает ;цель из прагмы).
- Замер _ш5м (точный SGD): старый код 14.76 с, новый 13.78 с
  (~7%); точность идентична (465/480, 733/800). Замеры на машине
  шумные — сравнивать только парами в один прогон.
- Внутренний цикл свертка_1_32: ~57 -> ~49 инструкций.
- Замечено: .рми-кэш хранит готовые фрагменты кода модулей —
  при изменении ген.раз промежуточные самN.иск могут содержать
  фрагменты старого стиля; критерий сида — побайтное равенство
  его собственной регенерации.
- Этап А, пункт А1 (ген.раз, индекс_адрес): прямая SIB-адресация
  для простых оснований. Новые хелперы: индекс_база (имя-основание
  -> регистр базы + дисплейсмент: рег-указатель -> БАЗ/Р12..Р15,
  указатель в слоте -> ПЕР СЧТ,слот, массив/век в кадре -> УКК±смещ)
  и адр_индекс (АДР АКК,[база + АКК*масш + дисп]; масш 2/4/8 — одна
  инструкция, прочие степени — СДЛ+АДР, 3/5/9 — LEA-трюк, иначе
  УМН+АДР). Срезы/строки и выражения-основания — общий путь со
  стеком (_пиши_адр_сум). Проверки границ не затронуты — идут до
  формирования адреса.
- Внутренний цикл слой1_прямой: 30 -> 28 инструкций, стековый
  пробег по обоим адресам устранён: ПЕР АКК,БАЗ / СДЛ АКК,4 /
  АДР АКК,[Р13+АКК].
- Замер _ш5м парой: старый 14.41/13.62 с, новый 13.78/12.73 с
  (~5-7% к версии А2+А4). Точность идентична (класс 7 -> 7, 98%).
- Фикс-точка (сам1==сам2), нативный прогон 111/111.
- Этап А, пункт А3 (ген.раз): прямые условные переходы. Новые
  усл_прыг/сравни_операнды/_усл_обрат/_усл_пер — сравнение в условии
  если/пока/и/или эмитит СРАВ + jcc без УСТcc+БЕЗРАСШ+ПРОВ; и/или —
  короткое замыкание по меткам, не — обращение смысла. В значковом
  контексте и/или материализуют 0/1 на метках слияния. Вещественные
  и векторные сравнения оставлены на общем пути (NaN/маски).
  _сегм30м.иск: УСТcc/БЕЗРАСШ 368 -> 206. Проверка
  «ряд>=0 и ряд<64»: было ~10 инструкций, стало 4.
- Фикс-точка нового кодогена (сам2==сам4), прогон 111/111,
  вывод _ш5м побайтно идентичен, метрики _сегм30м совпадают
  с записанными (эпоха 0: 0.501/0.108; эпоха 1: 0.233/0.221).
  Сиды разум.exe/разум/разум_мак пересобраны.
- Этап А, векторные временные (ген.раз, бин_век): коммутативные
  поэлементные операции (+, *, &, |, ^) используют спиленный левый
  операнд как источник m128 — «ОП ПЛАВ0, ОКВАД[УКС-16]» вместо
  ПЕРПС ПЛАВ1,ПЛАВ0 + ПЕРПС ПЛАВ0,[УКС]. Некоммутативные (-, /,
  сравнения) остаются на ПЛАВ1-маршруте. Внутренний цикл
  слой1_прямой: 30 -> 21 инструкция. Фикс-точка, прогон 111/111,
  сиды обновлены.
- Исправление этапа А5 (ген.раз): ОКВАД-спилл по УКС ломался при
  нечётной глубине стека — movups-запись терпит невыравненность,
  а «ОП ПЛАВ0, ОКВАД[УКС-16]» требует 16-байтового выравнивания
  (segfault в тесте век: кадр + нечётное число ВСТЕК). Теперь при
  ген.глуб % 2 != 0 резервируется 24 байта вместо 16 — слот остаётся
  выровненным. Та же поправка в интринсиках макс/мин.
- Этап Б (узлы.раз, парсер.раз, ген.раз): интринсики макс(век,век)/
  мин(век,век) — одна инструкция МАКСПС/МИНПС/МАКСПД/МИНПД с
  m128-источником. релу4 в _сегм30м переписан как
  «макс(вход, повтор(0.0))» — 12 скалярных ветвлений заменены
  одной пакетной операцией. Замечено: литерал 0.0 в повтор
  выводится как вещ.верх — в контексте без ожидаемого типа нужен
  «0.0 как вещ.рост».
- Фикс-точка (сам3==сам4), прогон 111/111 новым кодогеном,
  _ш5м: 7243 -> 6977 строк .иск, ПЕРПС ПЛАВ1-перетасовки 28 -> 4.
- Этап А6 (ген.раз): присв_слот — присваивание/составные операции по
  простому имени локала или параметра эмитят операцию прямо по слоту
  [УКК±смещ]: «накоп += выр» теперь ПЕРПС ПЛАВ1,[УКК-64] + ДОБПС +
  ПЕРПС [УКК-64] вместо АДР+ВСТЕК+ИЗСТЕКА+косвенности. То же для
  целых (ДОБ/ВЫЧ/И/ИЛИ/ИИЛИ/УМН/ДЕЛ по памяти) и вещей. Внутренний
  цикл свёртки: ~45 -> 41 инструкция. Фикс-точка, прогон 111/111,
  вывод _ш5м идентичен, сиды обновлены.
- Этап А7 (ген.раз): деление и остаток на степень двух сдвигами.
  x/2^k знаковое: bias-компенсация (СДПА 63 + СДП 64-k + ДОБ +
  СДПА k) — семантика усечения к нулю как у idiv, но ~30 тактов
  быстрее. Беззнаковое: СДП. x%2^k: маска через СДЛ/СДП-пару
  (беззнаковое) или bias + И -2^k/сдвиги (знаковое). /1 — без
  инструкций, %1 — ноль. Фикс-точка, прогон 111/111, сиды.
- Этап А8 (ген.раз): усл_граница — «a >= 0 и a < K» / «<= K» в любом
  порядке сводится к одной беззнаковой проверке без(a) < K
  (ВЫШРАВ/ВЫШЕ/НИЖЕ/НИЖРАВ). K только константа: переменная могла
  бы быть отрицательной. «a >= 0 и a < 0» — всегда ложь (ПРЫГ).
  Двойная проверка границ свёртки: 4 инструкции -> 2.
  Фикс-точка, прогон 111/111, сиды обновлены.
- Подготовка к этапу В — ручной вынос инвариантов в _сегм30м.раз
  (прецедент LICM до компиляторной реализации): в свертка_1_32,
  свертка_пак, макс_пул2, вверх2, соедини, выход_масок, гр_свертка,
  гр_свертка_1_32, гр_макс_пул2, гр_соедини, гр_вверх2,
  гр_выход_масок основания адресов подняты на уровень инвариантности
  (ряд*ширина, пиксель*блоков, весовые основания); весовые индексы
  вида ((((ю*3+ух)*вб+вбл)*4+лейн)*выб+вых) раскрыты в
  осн + вбл*4*выб + лейн*выб — только сложения во внутренних циклах.
  В гр_макс_пул2 четыре вектора-соседа грузятся один раз на блок,
  а не по лейну (16 загрузок -> 4). Внутренний цикл свертка_1_32:
  ~35 -> ~30 инструкций, убраны УМН АКК,АКК,3 и пересчёт
  вход_у*64. Полный прогон 6x24 сцены: ~23 мин (база до
  ROAD_MAP_2) -> 14м19с; стоимость ~9.4 с/сцена -> ~6.0 с/сцена
  (~1.6x суммарно). Вывод побайтно идентичен: эпохи
  0.501/0.233/0.205/0.245/0.189/0.240, IoU
  0.108/0.221/0.276/0.257/0.290/0.278, контроль BCE 0.086,
  IoU0=0.897, средний IoU 0.299.
- Этап В1 (ген.раз): индукционные переменные для `для` по
  диапазону. план_индукция в скан_тело (до пролога — слоты
  индукции входят в кадр): кандидат — `указатель[А + сч*Б]`
  по сырому `*Т`, требования: по возрастанию, шаг `+= 1`,
  счётчик не мутирует в теле, выражение инвариантно
  (инвар_выр: только конст/безмутабельные локалы и параметры,
  без вызовов и обращений к памяти; отсекаются и счётчики
  вложенных циклов). План идёт в скрытый слот
  `имя$индN` (`+адрес` = бегущий указатель, `-` = скаляр):
  пролог считает `указатель + (А + нач*Б)*разм` целиком —
  инвариантные части не тратят инструкций в цикле, на шаге
  одно `ДОБ слот, Б*разм` (или ПЕР для размеров вне 1/2/4/8).
  В индекс_адрес хук подменяет узел-объект на имя-адрес.
  Выигрыш в _путь_модуля самого компилятора: кадр 32→48,
  `УМН+АДР` на итерацию → `ПЕР АКК,[УКК-40]+ДОБ[УКК-32]`.
  Чисто инвариантный индекс получает слот без шага —
  заодно адресный LICM. Проверки: сборка, фикс-точка
  самосборки (сам3==сам4 после чистки .рми), тест
  указатель+массив → 240 верно, прогон 111/111.
- Этап Б3 (узлы.раз, парсер.раз, ген.раз): интринсик
  `выбери(маска, а, б)` — лейн из «а» где маска -1, из «б» где 0;
  б ^ ((а ^ б) & маска) в 4 SSE-инструкции с двумя спиллами
  (одно резервирование 32/40 байт — оба слота выровнены).
  Типизация: ветви — одинаковые векторы, маска — вектор того же
  байтового размера. Битовые `&`,`|`,`^` между векторами одного
  размера с разными лейнами разрешены (тип результата — левый):
  `д4 & (акт4 > повтор(0.0))` работает без каста. релу_гр4 в
  _сегм30м переписан: 4 извлечения + 4 ветки + сборка вектора
  (~40 инстр) -> СРАВПС + ПИ (~20 инстр на функцию). Проверки:
  тест маски и выбери верен, фикс-точка сам3==сам4, прогон 111/111.
- В3+Б2-часть (ген.раз): константный индекс сворачивается в
  дисплейсмент — «а[3]» по локалу/параметру: «АДР АКК,[УКК-40]»
  вместо вычисления индекса в АКК + SIB. Работает и для лейнов
  вектора «в[2]». Общий путь со стеком получает «ПЕР АКК,конст».
  Литералы с полным покрытием (век, массив целиком, запись
  позиционно целиком) не эмитят цикл обнуления: век {a,b,c,d}
  терял 16-итерационный ПЕР БАЙТ цикл перед записью лейнов.
  Фикс-точка сам3==сам4, прогон 111/111, сиды обновлены.
- Бенчмарк В1 (_сегм30м, полный прогон 6x24+контроль): 14м19с ->
  13м43с с индукционными адресами; вывод побайтно идентичен эталону
  (эпохи/IoU/BCE те же). Индукция сработала и в горячих циклах
  свёрток: веса[вес_осн + ядро_х*8] -> бегущий адрес, шаг 128;
  вход[вх_осн + ядро_х] -> бегущий, шаг 4. Внутренний цикл
  свертка_1_32: загрузки по слотам без УМН/АДР на итерацию.
- А5+ (ген.раз): прямой m128-источник для векторных бинопов.
  Операнд-имя вектора или индексация с векторным результатом
  (век_адресуем) не спиливается: второй операнд в ПЛАВ0, этот
  читается «ОКВАД[АКК]» — минус ВЫЧ/ПЕРПС/ДОБ УКС и перезагрузка.
  век_сравн получил параметр ист: «<»/«==»/«!=» читают m128
  напрямую, «>»/«>=» грузят в ПЛАВ1. Для выравнивания m128
  слоты век и массивов веков (разм%16==0) теперь выравниваются
  по 16 в кадре — и локали, и домашние слоты век-параметров.
  Поля записей и массив-параметры исключены (без выравнивания).
  Внутренний цикл свертка_пак: 14 -> 10 инструкций.
  Фикс-точка сам3==сам4, прогон 111/111, сиды обновлены.
- Финальный прогон _сегм30м с векторным релу_гр4 (выбери):
  13м18с; вывод побайтно идентичен эталону. Итог цепочки
  ROAD_MAP_2 на нейросети: ~23 мин (база) -> 13м18с (~1.73x).
  Парный _ш5м: лучший 16.7/12.7, новый 14.0/13.4 — паритет
  в шуме (ядро _ш5м скалярное).

- Графика и GPU (фича/графика): новые модули биб — «холст»
  (программный буфер 0xAARRGGBB, примитивы, блит, PPM; кросс-
  платформенно, кейс в прогоне), «окно» (Win32-окно + StretchDIBits,
  WNDPROC на РАЗУМЕ через GWLP_USERDATA), «гл» (OpenGL: wgl-контекст,
  база 1.1, современные функции через wglGetProcAddress → «функ» →
  косвенный вызов — проверен glGenBuffers), «окл» (OpenCL: контекст,
  буферы, программы/ядра, N-мерный запуск — проверено на RTX 3070,
  ядро удваивает 64 float). На linux/macos — заглушки с понятной
  ошибкой: ИМПОРТ есть только в PE64. Демо: examples/{холст,окно,гл,
  окл}_демо.раз. Прогон 112/112.

- Exp_AI_GPU этап 1 (свертка_замер): OpenCL-ядро свёртки 3x3 по
  float4-блокам повторяет свертка_пак из _сегм30м; 4 формы сети,
  50 итераций, RTX 3070. Выход побайтно совпал с CPU-эталоном
  (макс_ош 0.000). Ускорение: 4x4 256->256 ~x480 (30200 -> 62 мс),
  16x16 64->32 ~x257, 32x32 32->16 ~x253, 64x64 8->8 >x1900
  (упор в накладные запуска). Найдено: [лейн] у загруженного из
  указателя вектора даёт сбой — сверять плоским *вещ.рост.
  Также исправлен баг окно.windows: поля BITMAPINFOHEADER писались
  со сдвигом (biPlanes->biBitCount) — StretchDIBits давал белый экран.

- Exp_AI_GPU визуализация (визуал_ш5м v2 + биб/уй): интерактивная
  среда обучения _ш5м в окне 1280x800. биб/уй.раз — UI-набор поверх
  холста: растровый шрифт 5x7 (ASCII+кириллица), уй_текст/число/вещ,
  кнопки, панели, полосы, зоны клика. Управление: кнопки + пробел
  (пауза) + стрелка вправо (шаг), темп x1/x4/x16, сброс. Клик по
  узлу → инспектор: карта входных весов и активация нейрона.
  Метрики: эпоха/пример/точность/лосс/прим-с/кадр-с/время.
  Теплокарта вероятностей 64x8 + кривая точности по времени.
  Проверено: окно живёт, обучение идёт (эпохи 0-3: 197->447/480).
  Найдено: общая область функции — переменная век-типа в одном блоке
  ломает `для` с тем же именем («вектор и скаляр» в другой строке);
  динамический лейн у загруженного вектора не компилируется — читать
  через плоский *вещ.рост.

- Exp_AI_GPU этап 2 (форвард_гпу): прямой проход _сегм30м целиком на
  GPU. 6 ядер OpenCL (conv1/conv/pool2/up2/cat/outm) над двумя аренами
  буферов: веса (~124 МБ) и тензоры (~8 МБ); слои адресуются смещениями
  о_*, общими для CPU-эталона и GPU-цепочки. Сигмоида в outm повторяет
  Тейлора эксп4. Результат RTX 3070: 1391 мс CPU -> 15 мс GPU (~x87),
  все 23 тензора побайтно идентичны (FP_CONTRACT OFF). Найдено:
  `указатель + n` — байтовая арифметика (не по элементам!) — смещения
  арены через `ар()` на *нат.старт.

- Exp_AI_GPU визуализация (визуал_сегм): интерактивный прямой проход
  _сегм30м на GPU в окне 1280x860. Карта U-Net: энкодер слева вниз,
  декодер справа вверх, skip-нити горизонтально. 23 узла — плитки
  карт признаков (тепловая палитра), неготовые приглушены. Кнопки
  АВТО/СЛОЙ/СЦЕНА + темп x1/x4/x8, пробел и стрелка вправо. Инспектор
  по клику: до 32 каналов крупно + мин/макс/сред; вход — сцена 64x64,
  выход — 4 маски + наложение. Шкала времени 38 слоёв внизу. Диспетчер
  слоёв — таблица (вид/офс_вх/офс_вых/офс_вес/офс_см/ш/в/вхб/выхб).
  Проверено на RTX 3070: окно живёт, полный проход 15 мс за ~5 кадров
  (x4), плитки и маски отрисовываются, ~36-52 кадр/с.
  Фикс визуал_ш5м: СБРОС теперь обнуляет верно_эп (были значения вида
  641/480) и снимает выбранный нейрон.
  Дополнение визуал_сегм: объясняющий интерфейс — роли узлов текстом,
  строка состояния «слой N: вид · мс · дальше», имена видов слоёв без
  жаргона, подписи классов масок, легенда палитры, подсказки управления.
  Доработка визуал_сегм: раздельные «шаг» (посчитано на GPU) и «показ»
  (курсор просмотра) — НАЗАД/СЛОЙ ходят по истории прохода бесплатно,
  стрелки ←/→. АВТО — темп в слоях/с (1/с, 4/с, МАКС), не слоёв/кадр.
  Узлы с постоянной рамкой, под курсором светлеют + подсказка с именем.
  Инспектор автоматически следует за текущим слоем прохода.
  Проверено: стрелки ходят по истории, авто 4/с, инспектор следует.
=======
  Этап «мозг редактора» A+B (фича/редактор): Токен.путь/Лексер.путь,
  ошибки лексера и парсера печатают «<путь>: строка N:M: текст»;
  флаг разума --файл — виртуальный путь stdin для редакторов.
  Расширение 0.3: extension.js — живые диагностики из разум.exe
  (stdin, дебаунс 700 мс, ошибки взять-модулей в своих файлах),
  статус-бар, команда veles.check. Фикс-точка и прогон 93/93 на
  новом сиде; raz/разум.exe обновлён. Ошибки ген пока без файла
  (нужен Узел.путь — этап C).
  Этап C «мозг редактора»: ген_ош/ген_пред печатают ген.мод_тек —
  ошибки типизации теперь называют файл модуля (Узел.мод уже
  заполнялся загрузчиком). Исправлено смещение --файл: «--файл»
  это 10 байт UTF-8 (+пробел), а не 9 — срез попадал на второй
  байт «л» (0xBB), виртуальный путь превращался в «»». Фикс-точка,
  прогон 93/93, сид raz/разум.exe обновлён.
>>>>>>> фича/редактор

2026-09-23 — E2: инференс в редакторе (фича/мл-редактор)
- обуч_ош: выгрузка весов в ош_веса.bin после теста; баннер
  «обуч_ош v2» первой строкой; громкий фейл записи (код 7).
- ош_инф.раз: CPU-инференс, stdin «строка\nсообщение» → «вид <имя>»;
  баннер «ош_инф v1».
- Урок: запущенный exe блокирует перезапись — перед длинным прогоном
  проверять сборку: grep -ac "<строка>" инструмент.exe; путь записи
  проверен дымовым тестом (20 221 104 байт через файл_запиши).
- Расширение 0.4: [ML: <вид>] к первой ошибке, команда veles.ml.
- 2026-09-23: этапы Ж1–Ж2 (ветка фича/ипп). Фасад иск/ипп.раз —
  программный пайплайн ИСКРЫ (прагмы, ипп_план, ипп_образ,
  ипп_конвей, ипп_собери/ипп_собери_узлы); искра.раз — тонкий
  драйвер над ипп, вывод бинарно идентичен старому сиду
  (examples/привет.иск → cmp ок). *План.вид = ПЛАН_ФАЙЛ|ПЛАН_ПАМЯТЬ
  и *Дво.карта — закладки под JIT и отладочную карту. Разум берёт
  «взять иск.ипп»: режимы «разум файл.раз» и «-о» собирают образ
  в памяти — ни внешнего процесса искры, ни _запуск.иск.
  Проверено: самосборка, фикс-точка, прогон 112/112, запуск скрипта
  с ввод() и «-о» дают рабочий exe. Дизайн: docs/06-пайплайн.md.
