# лексер_бенч — замер скорости токенизатора: читает stdin, токенизирует,
# печатает только счётчики (вывод не замыливает замер).
взять основа
взять лексер

функ главная() цел64:
    исх := ввод()
    токи *Список = токенизируй(исх)
    печать("байт: ")
    печатьцел(исх.длина как цел64)
    печать("  токенов: ")
    печатьцел(токи.длина как цел64)
    печатьстр("")
    верни 0
