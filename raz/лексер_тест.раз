# лексер_тест — проверка токенизатора на РАЗУМЕ: читает исходник из stdin,
# печатает виды и тексты токенов. Временный файл, не часть компилятора.
взять основа
взять лексер

функ главная() цел.верх:
    исх стр = ввод()
    токи *Список = токенизируй(исх)
    для инд в 0..токи.длина:
        ток *Токен = список_получи(токи, инд) как *Токен
        печатьцел(ток.вид как цел.верх)
        печать(":")
        печатьцел(ток.знач как цел.верх)
        печать(":")
        если ток.текст == пусто:
            печатьстр("-")
        иначе:
            печатьстр(ток.текст)
    верни 0
