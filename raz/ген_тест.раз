# ген_тест — драйвер самораскрутки: stdin → разбери → генератор → stdout.
# Читает исходник РАЗУМА из stdin и печатает текст ИСКРЫ (.иск).

взять основа
взять лексер, узлы, парсер, ген

функ главная() цел.верх:
    исх стр = ввод()
    прог *Узел = разбери(исх)
    ген *Ген = ген_новый(0, 1, 1, 0)
    печать(ген.генерируй(прог))
    верни 0
