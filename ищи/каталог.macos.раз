# каталог — временный файловый слой ИЩИ для macOS.
# Прямые пути к файлам работают; рекурсивный getdirentries64 — следующий этап.

взять основа

функ каталог_собери(путь стр, рекурсивно лог, файлы *Список) лог:
    список_добавь(файлы, путь как нат.верх)
    верни да
